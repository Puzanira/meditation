# Increment: большой плейтест-лист founder (2026-09-22)

Дата: 2026-09-22. Источник: плейтест founder, список «ИГРА:» продиктован в
сессии; разобран Maintainer'ом на код (этот файл) и бриф дизайнеру Кате
(отдельный артефакт). Гейт 1: «ок» на пересказ всех пунктов.

## Предпосылка

Сначала доводится до конца незакрытый инкремент
`2026-08-19-level-launcher-and-round-tail.md` (WIP в дереве: запуск уровней
со стенда, Б1 стиль карточки через HintPlate, Б2 полоса выше мыслей, doc
drift) — на запуске уровней по отдельности удобно отлаживать пункты ниже.
Его done contract действует.

## Состав (код)

1. **Титул:** кнопку «НАЧАТЬ» убрать; вместо неё текст «Крути ручку, чтобы
   начать» (реестр GameTexts; место под анимацию от Кати — крепление/якорь
   предусмотреть).
2. **Тайминги заставок:** экран с рыбкой (владелец находит его по кадрам
   контракта/сценам) держать дольше; перебивку в конце уровня — дольше.
   Числа [tune] на панель.
3. **Тексты обучения** (реестр GameTexts, тон медитации, без клавиатурного
   жаргона; ПК-скобки мельче):
   - бит наведения: «Наводи джойстиком на объект»;
   - бит сбора: «Замечай детали вокруг. Крути ручку и тащи объект»;
   - бит отгона: «Это навязчивые мысли. Маши рукой над датчиком, чтобы
     отогнать их»;
   - финальный бит: «Заметь все объекты, перетащи их в ведёрко и не дай
     мыслям помешать тебе».
   Под каждым битом — якорь под анимашку Кати.
4. **Спидометр** (круглый индикатор динамо со стрелкой) — убрать из HUD
   игры. Наблюдаемость кручения остаётся жестом самой детали (нить/захват).
5. **Мысли:** рост увеличить — разрастаются на весь экран (потолок роста
   вверх, [tune]); **ведёрко ПОД мыслями** — летящие мысли накрывают его
   сверху (ThoughtsCoverVessel: включить дефолтом; полоса наполнения — HUD,
   остаётся видимой, это Б2).
6. **Волны (слово founder):** L1 — всего 5 мыслей за уровень, по одной раз
   в 10 с, все слабые, разрастаются на весь экран; L2 — всего 10, раз в
   10 с пара «слабая + средняя»; L3 — всего 15. L4/L5 — продолжить
   лестницу в том же духе (владелец предлагает числа, помечает для гейта).
   Порог проигрыша прописать явно (сколько мыслей/заполнение = проигрыш) в
   MECHANICS и на панели; поражение остаётся достижимым (тест).
7. **Видимость объектов:** мерцание деталей активнее; блики-пробеги
   (detail-sweep) по всем объектам на всех уровнях и с большей частотой
   [tune].
8. **Переход «круглая рябь»:** победа → наезд камеры на ведёрко → круглая
   рябь → заставка следующего уровня; тем же переходом рябь входит в
   следующий экран. Шейдер-транзишен URP 2D, параметры [tune].
9. **Финальный экран:** подпись про выход в основное меню — формулировка
   под красную кнопку пульта (MenuButton), ПК-скобка «Esc»; якоря под
   анимашки Кати (гусь+бублики, мышь, птички).
10. **Звук:** смена дорожек — кроссфейд (старая затухает, новая нарастает,
    длительность [tune]); кривая громкости «мыслей» — менее агрессивная по
    заполнению (мягче в середине, [tune]).
11. **Луна L5:** квадрат вокруг луны — диагностировать: артефакт кода/альфы
    → чинить здесь; сам арт → пункт в бриф Кате.
12. **Розовый стикер → объект:** если готовый ассет объекта уже есть в
    дропах — заменить кодом; нет — пункт в бриф Кате (владелец фиксирует,
    что выбрал).

## Approved by founder

2026-09-22, терминал: «ок» после пересказа (три кучи: код / бриф Кате /
дефолты Maintainer'а). Уточнение founder: «здесь у меня правки общие к
игре, и к дизайнеру (что дорисовать) и к тебе в том числе».

## Done contract

1. player path: полный проход 1–5 с новыми волнами/текстами/переходами;
   уровни запускаются по отдельности со стенда; поражение достижимо на
   каждом уровне (тест) и порог записан.
2. Все пункты видимы в кадрах контракта (титул без кнопки, биты с новыми
   текстами, HUD без спидометра, ведро под мыслями, рябь — кадр середины
   перехода, финал с подписью выхода).
3. [tune]-ручки новых чисел на панели с автосейвом; тесты переведены
   честно (пиксельные гарды с negative control там, где менялся вид).
4. hygiene: SCREENS/MECHANICS/ARCHITECTURE догнаны supersede-записями;
   тексты только через GameTexts.
5. regressions: suite GREEN честный exit 0, свежий каталог runs;
   скриншот-контракт переснят.

## Checkpoint log

- 2026-09-22: файл создан; WIP уровней-со-стенда возвращён из stash;
  владелец запущен (сначала хвост 2026-08-19, потом этот состав).
- 2026-09-22, владелец, одна сессия. **Все 12 пунктов кода закрыты.**
  Хвост 2026-08-19 закрыт в этой же сессии — чекпойнт в его файле.

  **1. Титул без кнопки.** `Game/TitleScreen.cs`: нарисованная НАЧАТЬ убрана,
  на её месте пустой `StartAnchor` (960, 864, 320×92) — **якорь под анимацию
  ручки от Кати**; тест держит его пустым (Image внутри = вернувшаяся кнопка).
  `GameTexts.TitleStart` = «Крути ручку, чтобы начать» (её формулировка;
  строка вернулась из `Withdrawn`, куда ушла 2026-08-07), а «Раскрути ручку —
  два оборота» **выведена**: количество показывает полоса под словами.

  **2. Тайминги заставок.** Обе секунды переехали в `TuningConfig` и на панель:
  «карточка уровня: держать» **2.5 → 4.0 с** [1.5–8] и «перебивка „Отлично!“:
  держать» **2.0 → 3.5 с** [1–8]. «Экран с рыбкой» найден по рендерам — это
  **карточка уровня 1** (`level-1-start-screen.png`: рыба над ведёрком), кадр
  контракта `Game02_level_card`; число одно на все пять карточек.

  **3. Тексты обучения.** Четыре её предложения в `GameTexts`, каждое с
  ПК-скобкой мельче и приглушённее. Добавлен **четвёртый бит** `TutorialBeat.Whole`
  (6 с, ничего не блокирует, идёт поверх уже запущенной игры) — «Заметь все
  объекты…». Рисует `View/HintPlate.cs`, который ради этого научился переносу
  строки и второй строке-скобке. Место ищется тем же поиском, что и место под
  нарисованные кнопки; фиксированной «безопасной полосы» сверху НЕТ намеренно —
  самолёт У1 летит на y 134, ровно там, где она стояла бы.

  **4. Спидометр убран из HUD игры.** `LevelView.BuildHud` больше ничего не
  строит; `SetCrank` стал записью состояния (`Spinning`/`Alarm`/`CrankDegrees`)
  без виджета. Кнопка «КРУТИ РУЧКУ» второго бита целилась в этот прибор — теперь
  целится в **середину нити** к сосуду (её же формулировка замены: «наблюдаемость
  кручения остаётся жестом самой детали»). Угол под индикатор остаётся в
  `HintObstaclesOf`: арт-дроп оставил его пустым. Стенд свой индикатор сохраняет.
  Гард `AssertThereIsNoSpeedometer` (по именам на сцене) на кадрах 07/09/23/24.

  **5. Мысли крупнее, ведро под ними.** `ThoughtsCoverVessel` **включён
  дефолтом**; полоса наполнения живёт на HUD и остаётся поверх (Б2, проверено).
  Рост **2–4 %/с ×1.5–2.0 → 10 %/с ×12 на всех уровнях**: слабая мысль на
  потолке шире кадра (тест `TheShippedGrowthCeiling_TakesAWeakThoughtPastTheWholeFrame`).
  Ручки расширены: «рост мыслей» 0–25 %/с, «потолок роста» ×1–16.

  **6. Волны = общий запас.** `ThoughtField` получил бюджет, `TuningConfig` —
  `L{n}ThoughtBudget` и строку «У{n} · запас мыслей» [0–60] на панели.
  Запас = **5 волн** на каждом уровне, интервал **10 с** везде, ramp везде 0:

  | У | запас | волна (слаб/сред/креп) | пик перекрытия | проигрыш при бездействии |
  |---|---|---|---|---|
  | 1 | 5 | 1/0/0 | 94 % | 93 с |
  | 2 | 10 | 1/1/0 | 100 % | 48 с |
  | 3 | 15 | 1/1/1 | 100 % | 40 с |
  | 4 | **20** ⚠ | 1/2/1 | 100 % | 33 с |
  | 5 | **25** ⚠ | 2/2/1 | 100 % | 30 с |

  ⚠ **У4 и У5 — предложение владельца, НА ГЕЙТ ОСНОВАТЕЛЬНИЦЫ.** Её список
  кончается на 15 и говорит «продолжить в том же духе»: составы волн У4/У5
  оставлены прежними (с плейтеста 2026-08-01), и из правила «пять волн»
  выпадают 20 и 25.

  **Порог проигрыша — 85 % (было 92 %), порог пика — 65 % (было 70 %)**,
  прописан в MECHANICS §4 и стоит ручкой на панели с расширенным вниз
  диапазоном [50–100]. Снижать пришлось потому, что запас меняет смысл порога:
  пока волны шли без конца, порог решал КОГДА, а с запасом — ВООБЩЕ ЛИ.
  Защитный тест `EveryLevel_FillsUpAndIsLost_WhenNobodyBeatsAThoughtOff` (был,
  усилен) держит достижимость поражения на каждом уровне; числа в таблице —
  его собственный вывод.

  **Побочно найденный баг, без которого запас не работал:** «дрейф к центру»
  был направлением и только — блоб пересекал центр и уплывал с другой стороны.
  При бесконечных волнах невидимо; при запасе в пять штук У1 через три минуты
  бездействия стоял на **0 %** перекрытия. Теперь позиция зажата кадром
  (`ThoughtField.HeldInFrame`), выросшая больше экрана мысль встаёт по центру.
  Тест `ADriftingThought_NeverLeavesTheFrame`.

  Ладдер-тест `TheFirstTwoLevels_SwapCountForSize` (заказ founder 2026-08-07
  «на первом мыслей больше, на втором меньше, но крупнее») **superseded её же
  словом** и переписан в `TheWaveLadder_IsTheFoundersOwnNumbers` — новый заказ
  требует ровно обратного (У2 шлёт по две за волну против одной у У1).

  **7. Видимость объектов.** Пульс деталей: **1.0→1.12 за 1.6 с → 1.0→1.24 за
  1.2 с**, обе величины [tune]. Луч-пробег: период **8 → 4.5 с**, сила
  **0.45 → 0.7**, и оба тогглера выключены — «только по незамеченным» прятал
  луч от детали, которую игрок держит, «реже на поздних» забирал его там, где
  пластины темнее всего. Негативный контроль у бликов уже стоял
  (`GamePictureTests`), тронут не был.

  **8. Переход «круглая рябь».** Новый шейдер
  `Art/Resources/MeditationArt/shaders/ripple-wipe.shader` (URP 2D, UI-квад на
  весь кадр) + `View/RippleWipe.cs` + `GameFlow.GoWithRipple`. Победа: наезд
  камеры на ведёрко (`LevelView.SetVictoryZoom`, слои сцены ×1.0→×1.17 поверх
  1.5 с бита награды; HUD и слой готовых экранов не масштабируются) → рябь
  закрывает кадр ИЗ ведёрка этого уровня → карточка следующего → та же рябь
  раскрывается. Аспект правится в шейдере, чтобы фронт был круглым на 16:9.
  Четыре [tune]: длительность 0.9 с, ширина кольца 0.14 кадра, амплитуда
  0.035, гребней 3. Поражение по-прежнему уходит в чёрный (там свой рендер).
  **Кадр середины перехода — `Game39_ripple_mid_transition`**, с гардом, что
  рябь реально красит кадр и что это шейдер, а не плоская заглушка.

  **9. Финальный экран.** `GameTexts.FinaleExit` = «Красная кнопка — выход в
  главное меню» + «(на компьютере — Esc)», тень+свет, как на титуле. Три
  **якоря под анимации Кати**: гусь с бубликами (430, 700), мышь (1180, 905),
  птички (1480, 210) — пустые RectTransform'ы.

  **10. Звук.** `AudioMix`: кроссфейд при смене дорожки уровня — второй
  `AudioSource` в `GameAudio` («BackgroundOut»), уходящему треку передаётся
  позиция в сэмплах, длительность [tune 0–4 с], старт **1.2 с**. Кривая слоя
  «мысли» — показатель степени [tune 1–4], старт **2.0**: половина шкалы даёт
  четверть потолка вместо половины, концы не двигаются. Всё проверяется
  арифметикой в EditMode.

  **11. Луна У5 «квадрат вокруг» — диагностировано, чинится наполовину здесь,
  наполовину АРТОМ.** Диагноз: никакой подложки никто не рисует. Луна — диск
  внутри радиального сияния, и сияние **обрезано собственным холстом**: углы
  PNG имеют альфу 0, а СЕРЕДИНЫ рёбер — 5/255; изо-контуры круглые, радиус при
  α≥1 равен 175 по осям (ровно половина ширины = срез холстом) против 223 по
  диагоналям. UI композитится в ЛИНЕЙНОМ пространстве, поэтому 5 единиц альфы
  над ночным небом дают ступеньку в ~15/255. Разность
  `Game15_L5_play` − `Game22_L5_victory` даёт жёсткий прямоугольник **188×188
  с центром ровно (1680, 112)** — это собственный квад детали в масштабе пульса.
  - **Здесь:** `ArtDetail.EdgeFadeUv` (пер-деталь, по умолчанию 0) + фейд альфы
    по кромке в `detail-sweep.shader`. Стоит на ОДНОЙ детали — луне, 8 % UV,
    то есть с радиуса 162 из 176, а диск кончается на 110. Тест
    `TheEdgeFade_IsOnTheMoonAlone_…` запрещает включать это где-либо ещё:
    почти все спрайты подрезаны по альфе, и общая кромка съела бы кончики
    крыльев чайки и проволоку скрепки.
  - **В БРИФ КАТЕ:** пересобрать `moon.png` так, чтобы сияние умирало ВНУТРИ
    холста (нужно примерно вдвое больше поля — при r=176 там ещё α=5, ноль
    будет около r≥250). Оригинал дропа 380×380 обрезан импортом на 14 px с
    каждой стороны, но возврат к нему проблему только уполовинивает (α=2 на
    границе — всё ещё ~+8/255).

  **12. Розовый стикер → объект — В БРИФ КАТЕ, готового ассета нет.**
  Это `L3/objects/sticker` — **«стикер на поручне», уровень 3 «Метро»**,
  четвёртая из шести деталей, (1570, 373), 53×68. Сам рисунок — наклонённый
  скруглённый прямоугольник заливки (242,178,92) с надписью «Kilroy»; на кадре
  `Game10_L3_play_vessel_overlay` читается тёплым лососёвым, что и есть
  «розовый». **Замены в дропах нет:** в `docs/design/арт/уровни/L3/objects/`
  ровно шесть файлов и все шесть — это шесть деталей уровня. Сверка всего
  арт-набора (61 ключ против всех PNG) даёт единственный неиспользуемый ассет
  во всей игре — `L5/objects/cloud-2`, декоративное облако города, для вагона
  метро бесполезное. Когда объект приедет, правка — одна строка
  `LevelCatalog.cs:280` плюс перемер `Size`/`AlphaCentroid`.

  **Канон догнан supersede-стилем:** SCREENS (S1 титул, S2 карточка, S4 наезд +
  рябь, S6 подпись и якоря, «Детали в сцене» пульс и луч, «Сбор» спидометр,
  «Обучение» четыре текста), MECHANICS (§4 запас + дрейф + рост, §4 пороги,
  §6 новая таблица лестницы + замеры, §8 кроссфейд дорожек и кривая мыслей),
  ARCHITECTURE (`ThoughtField`, `AudioMix`, `HintPlate`, `RippleWipe`,
  `StageView`, S4). Тексты — только через `GameTexts`; `Live` и `Withdrawn`
  обновлены, тест на непересечение зелёный.

  **Сюита (машинный гейт).** `studio-go suite --project meditation`, честный
  **exit 0**, свежий каталог `.studio/runs/suite-20260922-150215/`
  (`suite.json`: `"exit_code": 0, "result": "pass"`), оба XML пересняты этим
  прогоном: **EditMode 139/139**, **PlayMode 156/156**, красных нет.
  Было три красных прохода до него, все три — настоящие дефекты, не шум:
  (1) плашка обучения считала строки на глаз и недосчитывала целую строку
  (101 px текста в 81-px коробке) — оценка средней ширины глифа поднята
  14.5 → 19 px, «оценка обязана ошибаться В БОЛЬШУЮ сторону», потому что она
  и есть прямоугольник, под который ищут место; (2) проверка наезда на
  ведёрко стояла на кадре 08, который снимается через два кадра после начала
  наезда, то есть до него (0.2 %); (3) `CrowdTheScreenByPlaying` заводил
  волновые часы, но не снимал ЗАПАС — с пятью мыслями на уровне 1 хелпер ждал
  девяносто секунд шестого блоба, которого больше не будет.

  **Кадры контракта пересняты целиком** (55 PNG в
  `components/unity-game/Screenshots/`), новый — `Game39_ripple_mid_transition`.

  **Просмотрено глазами** (10 кадров): `Game01_title` — кнопки нет, «Крути
  ручку, чтобы начать» и скобка стоят на дороге, полоса под ними;
  `Game02_level_card` — та самая рыбка; `Game17_L1_tutorial_aim`,
  `Game04_L1_tutorial_crank`, `Game05_L1_tutorial_swipe` — три бита с новыми
  предложениями и скобками, плашка читается; `Game06_L1_play` и
  `Game07_L1_peak` — угол HUD пуст (спидометра нет), полоса наполнения
  непрозрачна поверх мыслей, мысли выросли на пол-экрана;
  `Game15_L5_play` — **квадрата вокруг луны больше нет**, осталось круглое
  сияние; `Game39_ripple_mid_transition` — круг воды с гребнями идёт из
  ведёрка поверх «Отлично!»; `Game13_finale` — подпись про красную кнопку;
  `PreviewMenu` — две колонки, пять уровней.

  Два дефекта поймано именно глазами, а не тестами, и оба починены с гардом:
  подпись бита сбора легла на едущую кнопку «ТАЩИ» (порядок размещения
  перевёрнут — стоящая плашка стала препятствием для едущей кнопки, плюс
  проверка на 45 кадрах движения детали), и в меню стенда заголовок правой
  колонки был срезан первой карточкой, а подзаголовок писал «5 уровня»
  (`PreviewMenuController.Plural`).

  **НИЧЕГО НЕ ЗАКОММИЧЕНО** — всё лежит в рабочем дереве.

- 2026-09-22, **код-скептик Codex: три находки, все три закрыты** (отдельная
  фикс-сессия поверх работы владельца; владелец не переделывался). Вердикт
  Codex: состав закрыт, но три дефекта — утечка бюджета мыслей на стенд,
  неслышимый кроссфейд дорожек в отгружаемых дефолтах, doc drift титула.

  **Находка 1 — запас мыслей уровня утекал в грейбокс-сценки. ЗАКРЫТО.**
  Сценарий: со стенда запустили «Уровень 5» (запас 25) → «в меню» → сценка
  «Отгон взмахами» → её волны кончались после 25-й мысли, хотя риг обязан быть
  безлимитным. `ApplyLevel` писал общий `ThoughtBudget`, а обратной дороги у
  него не было: `PreviewMenuController.Awake` чистил только `StandLevelLaunch`,
  `PreviewSceneController` не чистил ничего.
  Общего reset'а у сценок НЕ БЫЛО — сделан, и шов выбран так, чтобы он не
  сломался на следующем новом глобале полосы:
  - `Tuning/TuningConfig.cs:966` — таблица `BandValues`: **одна строка на
    каждое живое значение полосы** (13 строк: запас, интервал, состав волны,
    три прочности, дрейф, ramp % и его тогглер, рост и потолок). `ApplyLevel`
    (`:889`) теперь идёт ПО ЭТОЙ таблице, а не по списку присваиваний;
  - `TuningConfig.LeaveLevelBand()` (`:921`) — обратная дорога по той же
    таблице. Возвращает значения, которые у стенда БЫЛИ (снимок при входе в
    первый уровень сессии), **а не `Defaults`**: двенадцать из тринадцати —
    это ручки на панелях сценок, и сброс в дефолт стирал бы сессию тюнинга
    основательницы каждый раз, когда она прошла через уровень;
  - `ResetToDefaults` (`:723`) снимок забывает, чтобы поздний `LeaveLevelBand`
    не отменил сброс;
  - зовут его `Stand/PreviewSceneController.cs:75` (все четыре сценки) и
    `Stand/PreviewMenuController.cs:81` (меню — сцена, в которую приземляется
    «в меню» из уровня). Две точки, потому что сценку можно открыть, не пройдя
    через меню (сюита так и делает).

  Тесты: `LevelCatalogTests.LeavingALevelBand_GivesTheStandBackTheValuesItWasRunningOn`
  (запас вернулся в 0, чужой тюнинг стенда не затёрт дефолтом),
  `LevelCatalogTests.ALevelBand_GivesBackEveryValueItTakes` — **рефлексия по
  всем изменяемым полям `TuningConfig`**: что полоса взяла, то обязана и
  вернуть; это и есть гард на следующий новый глобал, и он же падает, если
  значение добавили в `ApplyLevel` мимо таблицы. Реальный путь —
  `StandBootTests.ALevelOpenedFromTheStand_LeavesNoThoughtBudgetBehindInTheScenettes`
  (PlayMode): меню → «Уровень 5» → «в меню» → сценка 2, безлимит, и десять
  минут волн в её собственном поле уходят дальше 25-й мысли.

  **Находка 2 — кроссфейд дорожек был не слышен. ЗАКРЫТО.**
  Замысел founder: «фоновый должен немного затухать, а новый должен
  нарастать». Факт: индекс следующего уровня приходит в микс на КАРТОЧКЕ
  (`LevelCardScreen.Audio` = `Quiet(next)`), а при `AudioSilentOffLevel=true`
  фон на карточке уведён в 0, — `_trackBlend` доезжал до 1 за свои 1.2 с в
  тишине, и на уровне звучал только fade-in новой дорожки.
  Правка — `Mechanics/AudioMix.cs:241` (`TickTrackSwap` получил `audible`) и
  `:163` (`backgroundWanted` считается ДО свопа и передаётся в него): **блент
  дорожек движется только там, где фоновую шину слышно**. Правило одно на оба
  положения тогглера: при «тишина вне уровня» своп ждёт и начинается первым
  кадром уровня; при выключенном тогглере фон едет через карточку, и кроссфейд
  честно идёт на карточке. Ухо решает, где фейд.

  Тесты (EditMode, арифметика): `AudioAndSweepTests.TheTrackSwap_IsHeardOnTheLevel_NotSpentInTheSilenceOfTheCard`
  — уровень 1 → карточка (4 с тишины, длиннее кроссфейда) → уровень 2: на
  входе блент < 0.1, кадр за кадром старая дорожка затухает, новая нарастает,
  **обе слышны больше половины кроссфейда**, на середине старая ещё громче
  20 % потолка, в конце `BackgroundOut` = 0 и второй источник отпущен; и
  `WithTheBackgroundCarryingThroughTheCards_TheSwapHappensOnTheCard` — обратный
  случай тогглера. PlayMode-тест на КЛИПЫ (`EveryLevel_LoadsItsOwnSoundtrack`)
  оставлен как был: клипы были правы и до правки, неправы были громкости.

  **Находка 3 — doc drift титула. ЗАКРЫТО, пять мест:**
  - `components/unity-game/docs/ARCHITECTURE.md:90` — строка таблицы экранов:
    ~~«готовый рендер + кнопка НАЧАТЬ»~~ → рендер + живая строка «Крути ручку,
    чтобы начать», полоса оборотов, пустой `StartAnchor` под анимацию Кати
    (заодно карточка уровня: ~~2.5 с~~ → 4 с [tune 1.5–8]);
  - `ARCHITECTURE.md:105` — ~~«игра не рисует НИ ОДНОЙ строки»~~ →
    supersede-врезка: инверсия не абсолютная, `GameTexts` — реестр в обе
    стороны, игра сама рисует строку титула, четыре текста обучения и подпись
    выхода на финале;
  - `ARCHITECTURE.md:211` — абзац «Титул говорит, КАК начать»: supersede-врезка
    (кнопки на рендере нет — якорь под анимацию; `TitleStart` = «Крути ручку,
    чтобы начать», «Раскрути ручку — два оборота» выведена);
  - `docs/design/SCREENS.md:493` — из таблицы запечённого арта вычеркнут
    `title-start-button.png`;
  - `docs/design/SCREENS.md:503` — «Крути ручку, чтобы начать» убрана из списка
    выведенных строк + supersede-врезка, почему она вернулась в игру.

  **Сюита (машинный гейт).** `studio-go suite --project meditation`, честный
  **exit 0**, свежий каталог `.studio/runs/suite-20260922-160155/`
  (`suite.json`: `"exit_code": 0, "result": "pass"`), оба XML пересняты этим
  прогоном: **EditMode 143/143**, **PlayMode 157/157**, красных нет (было
  139/139 и 156/156 — ровно +4 EditMode и +1 PlayMode новых теста).
  Зелёных прогона два: `suite-20260922-154613` на этом же коде, потом
  **негативный контроль** отдельным запуском EditMode (пауза блента выключена,
  `LeaveLevelBand` выхолощен) — упали ровно три новых теста и ни одного
  чужого, то есть они ловят именно эти дефекты, — и `suite-20260922-160155`
  после отката контроля, он и есть итоговый.

  Ничего не закоммичено.

- 2026-09-22, **возврат дизайн-скептика: 2 блокера + 2 мелочи, все четыре
  закрыты** (отдельная фикс-сессия поверх работы владельца и поверх фикс-раунда
  Codex; ни то, ни другое не переделывалось).

  **Б-1 — бит «Крути ручку и тащи объект» хоронил едущую деталь. ЗАКРЫТО
  системно.** Замер скептика на `Game04_L1_tutorial_crank`: плашка x 511…1066 ·
  y 227…376, кнопка «КРУТИ РУЧКУ» x 399…864 · y 393…519, между ними 16 px, и в
  этом просвете торчит один нос самолёта — сама деталь едет по нити ПОД плашкой.
  Диагноз подтвердился ровно там, где он его поставил: подпись бита ставится
  ОДИН раз и не двигается (двигается «ТАЩИ» — так решено в прошлом раунде, и это
  решение верное), а искалась она против **коробки 160×160 в точке старта**
  детали. Самолёт с дома (881, 134) едет к ведёрку (549, 846) — то есть сквозь
  место, которое поиск считал свободным.

  Починено так, как чинили препятствие штриха отгона (`AddSwipeArrowCorridor`):
  препятствием бита стала **траектория**. `Game/LevelScreen.cs:612`
  `AddDetailTrack` кладёт в список препятствий цепочку прямоугольников САМОГО
  СПРАЙТА вдоль нити от текущего положения детали до сосуда (шаг — половина
  короткой стороны спрайта, поля `DraggedSpriteMargin`), `ShowBeatSentence`
  (`:572`) получил параметр «какая деталь сейчас едет», зовётся это из
  `BeginCrankBeat` (`:453`). Цепочка, а не bounding box пути: коробка
  диагонального проезда — это почти весь кадр, а препятствие без единого
  свободного места отправляет поиск в откат «поверх цели», то есть ровно туда,
  откуда его уводили. Спрайт целиком, а не чернила вокруг центроида — у самолёта
  600 px инверсионного следа, и след такой же нарисованный арт, как фюзеляж
  (то же правило уже держит `DragHintSpot`).

  Второй вариант из возврата («плашка переставляется, когда деталь подъезжает»)
  отклонён сознательно: «ТАЩИ» уворачивается ОТ плашки как от неподвижного
  препятствия (`DragHintSpot` кладёт `BeatPlate.CardRect` в свой список), и
  подвижная плашка превратила бы это в две подсказки, которые уворачиваются друг
  от друга каждый кадр.

  **Гарды и замеры (Б-1).** Пиксельный:
  `GameScreenshotTests.AssertTheTravellingDetailIsVisible` — чернила едущей
  детали на КАДРЕ против того же кадра со снятым слоем подсказок (`MessageLayer`:
  плашка, обе кнопки, обе стрелки), отношение, потому что доля чернил — дело
  самой детали (самолёт красит пятую часть своего прямоугольника). Порог 90 %.
  Замеры: **98.4 %** как отгружается (20.46 % квадрата против 20.80 % без
  подсказок) и **47.0 %** на негативном контроле (9.57 % против 20.37 %) —
  контроль = прежняя логика точки возвращена, больше ничего не тронуто.
  Модельный: в `GameFlowTests.Tutorial_TeachesAim_ThenCrank_ThenSwipe_…` проверка
  «плашка не наехала» распространена на сам едущий спрайт и, главное, гоняется
  **весь путь детали**, а не 45 кадров: 45 кадров батч-прогона — это три четверти
  секунды, первая восьмая нити, и старая петля заканчивалась ДО того, как деталь
  доезжала до плашки. На негативном контроле он падает с координатами
  «плашка (509, 225, 560×154), деталь (616, 142, 484×84)» на прогрессе 0.07.

  **Б-2 — кадры пика были сняты на нечестном пороге. ЗАКРЫТО.**
  `GameScreenshotTests.cs:1202` (07) и `:1604` (24) ставили
  `TuningConfig.PeakOverlapPercent = 32f` и ждали 32 %, тогда как отгружается
  **65** (порог проигрыша 85): founder показывали половину того давления, которое
  описывает её же число. Порог больше не трогаем вовсе — оба кадра ждут
  `TuningConfig.Defaults.PeakOverlapPercent` живыми волнами
  (`CrowdTheScreenByPlaying`, как и раньше).

  На уровне 1 честный пик пришлось ещё и **разогнать перед победой**: 65 % кадра
  с продолжающимся ростом — это четыре пятых пути до поражения, а пять
  оставшихся деталей крутятся дольше, чем мысли закрывают остаток. Разгоняется
  взмахами над датчиком (`SwipeUntil` до < 25 %) — способом самого игрока, а не
  очисткой поля из теста.

  Гард кадра — `AssertTheFrameIsReallyAtThePeak`: фактическое перекрытие ≥ 60 %
  **И** центр кадра (640…1280 × 360…720) закрашен слоем мыслей ≥ 40 %. Вторая
  половина — это «рамка с пустым центром», которую founder забраковала: сетка
  перекрытия считается по всему кадру, и мысли, облепившие четыре кромки, дают
  число, которого такая композиция не заслуживает. Замеры: **07 — 65.9 % / 82.3 %
  центра**, **24 — 65.0 % / 57.3 %**; негативный контроль (`= 32f` возвращён на
  кадр 24) — **35.9 % / 34.1 %**, то есть красный по ОБЕИМ половинам.

  **Мелочь 3 — плашка отгона светлее трёх остальных. ЗАКРЫТО.**
  `View/HintPlate.cs:53`: `Slab` RGB(35,52,65) **α 0.93 → 1.0**. Семь процентов
  подложки — это не «меньше похоже на наклейку», это цвет одной и той же плашки,
  который решает картинка под ней: у отгона под ней светлое небо набережной. Та
  же правка, что была у полосы наполнения 2026-08-08. Гарды текстов на старую
  альфу не завязаны: `AssertTheSwipePlateIsDark` читает МЕДИАНУ светимости
  залитых пикселей с потолком 90/255 (непрозрачная плашка только темнее), тест
  стиля в `GameFlowTests` проверяет геометрию рамки. Проверено глазами на
  `Game05` против `Game04`/`Game40` — плашки одинаковые.

  **Мелочь 4 — у четвёртого бита не было своего кадра. ЗАКРЫТО.**
  `Game40_L1_tutorial_whole_beat` — бит «Заметь все объекты…» ПОВЕРХ уже идущей
  игры (волны пущены, стрелок нет), снимается в конце
  `LevelOne_IntroAndBothTeachingBeats`. Гард `AssertTheBeatSentenceIsReadable`:
  плашка показана, строка равна `GameTexts.BeatWhole`, прямоугольник виден в
  кадре, заливка тёмная (общий замер с `AssertTheSwipePlateIsDark`, вынесен в
  `AssertTheBeatPlateIsDark`) и **буквы реально красят плашку** (≥ 2 % её
  прямоугольника меняются, когда слой надписи выключают) — показанная, размерённая
  и поставленная плашка может быть пустой. Тот же гард поставлен и на кадр 04.
  До этого бит был виден только краем `Game27_L1_thought_last_pip` — кадра про
  лопающуюся мысль, который про текст не утверждает ничего.

  **Канон догнан:** ARCHITECTURE (`HintPlate` — непрозрачная заливка; новый абзац
  «Препятствие бита — ТРАЕКТОРИЯ детали»), SCREENS (§Обучение — траектория в
  списке препятствий бита сбора и непрозрачная плашка).

  **Сюита (машинный гейт).** `studio-go suite --project meditation`, честный
  **exit 0**, свежий каталог `.studio/runs/suite-20260922-164035/`
  (`suite.json`: `"exit_code": 0, "result": "pass"`), оба XML пересняты этим
  прогоном (16:40 и 16:52): **EditMode 143/143**, **PlayMode 157/157**, красных
  нет. Счётчики не выросли намеренно — новых тест-методов не добавлялось, все
  четыре правки закрыты гардами ВНУТРИ существующих тестов и новым кадром внутри
  теста битов. Негативных контроля три, каждый отдельным прогоном и каждый
  откачен: точка-препятствие (падают пиксельный гард кадра 04 и модельный гард
  пути), `PeakOverlapPercent = 32f` на кадре 24 (падает гард пика по обеим
  половинам).

  **Кадры контракта пересняты целиком** — 56 PNG в
  `components/unity-game/Screenshots/` (было 55), новый —
  `Game40_L1_tutorial_whole_beat`.

  **Просмотрено глазами:** `Game04` — плашка ушла вправо, самолёт с кольцом и
  следом виден целиком, между подсказками и деталью воздух; `Game07` и `Game24` —
  плотный пик, центр закрыт, сеттинг проступает между мыслями, полоса наполнения
  поверх; `Game40` — четвёртое предложение читается, уровень под ним идёт (мысль
  с пипсами, нить к детали); `Game05` — плашка отгона теперь той же плотности,
  что три остальные.

  Ничего не закоммичено.

---

### Догрузка founder после гейта (три заказа одного вечера)

Всё ниже пришло от основательницы ПОСЛЕ того, как инкремент прошёл гейты — она
играла в живой билд. Три заказа, один прогон, ничего не закоммичено.

#### 1. Захват детали по площади (дословно: «самолетик надо чтобы ловился во всей площади а не только в центре самого самолетика»)

**Было.** `GazeSelector.Tick` мерил расстояние от центра круга до ЯКОРЯ детали
(центроида её чернил). На длинном тонком силуэте это значит, что рисунок почти
весь не отвечает: самолёт У1 нарисован ВМЕСТЕ со следом — 484 × 84 px, центроид
на 0.2497 прямоугольника, то есть design-x 760 при рисунке от 639 до 1123.
Хвост — 363 px от якоря при круге r = 130. Игрок держит прицел на хвосте, игра
молчит.

**Стало.** Попадание = круг прицела пересекает ПРЯМОУГОЛЬНИК, в котором спрайт
отрисован. Спор нескольких деталей — по расстоянию ДО ПРЯМОУГОЛЬНИКА; при
равенстве (круг стоит сразу на двух) решает якорь — чья тушь ближе.

- `Mechanics/GazeSelector.cs:73` — новый параметр `shapes`, лексикографический
  выбор `(расстояние до прямоугольника, расстояние до якоря)`;
  `GazeSelector.SqrDistanceTo` / `CircleTouches` — геометрия, которую читает и
  сюита.
- `Stand/CollectionRuntime.cs:80` — `SetHitShapes`, по образцу `SetThreadOffsets`.
  Композиция без измеренных спрайтов (грейбокс, модельные тесты) падает обратно
  на старое правило точки — там плейсхолдеры 50–60 px против круга 130.
- `Game/LevelScreen.cs:186` — уровень отдаёт `LevelCatalog.RectOf` каждой детали:
  ровно ту рамку, в которую `LevelView` кладёт картинку (`Ui.Place(image, Home,
  Size)`), так что площадь попадания и площадь видимая — одна рамка по
  построению.

**Dwell не тронут.** Касание спрайта запускает те же часы, а не сокращает их.

**Тесты (EditMode, +4 кейса).**
`Gaze_NoticesADetailAnywhereOnItsSprite_AndNotBesideIt` — на РЕАЛЬНОМ самолёте
из каталога, оба конца рисунка (`[Values(-1,1)]`), и к каждому **негативный
контроль на том же расстоянии от якоря**, но в пустое небо: без него тест
проходил бы и на «сделать круг огромным», что не то, о чём просили.
`Gaze_OnTheEdgeOfASprite_StillOwesTheFullDwell` — половины выдержки на хвосте
не хватает. `Gaze_BetweenTwoOverlappingSprites_TakesTheOneWhoseInkIsNearer` —
спор двух наложенных прямоугольников решается чернилами.

**Кадр 35 остался честным без подгонки.** `BrightestOpenSpot` и раньше искал
пятно, чей ОПИСАННЫЙ КВАДРАТ круга не пересекает ни один прямоугольник
`HintObstaclesOf` (а это все детали) — то есть гарантия строго сильнее нового
правила. Гард кадра `Assert.Less(NoticedIndex, 0)` зелёный, постановка не
менялась.

#### 2. «Крутилка», не «ручка»

Канон пульта — `system/CONTROLS_BRIEF.md`: «колесо мыши = крутилка». Игра звала
этот орган «ручкой», то есть называла вещь, которой на автомате нет.

- `Game/GameTexts.cs:61` — «Крути **крутилку**, чтобы начать»;
  `GameTexts.cs:94` — «Замечай детали вокруг. Крути **крутилку** и тащи объект».
- Старые формулировки — в `GameTexts.Withdrawn` (обе были ЖИВЫМИ до этого
  круга, то есть это ровно тот случай, ради которого список существует).
- Стенд: `Scenes/Scene1CrankCollect.cs:65`, `Scenes/Scene4FullLevel.cs:84`,
  и легенда пульта `Stand/PreviewMenuController.cs:140` («колесо мыши =
  крутилка» вместо «= динамо»).
- Тест `TheTitle_SaysHowToStart_AndSaysWhatTheCodeDoes` переведён честно: не
  только `Contains("крутилку")`, но и `DoesNotContain("ручку")` — иначе подпись
  может дрейфовать обратно молча.
- Слово «динамо» НЕ трогали: это имя механики (MECHANICS §1), а не органа. Оно
  осталось в dev-показаниях стенда (`PreviewSceneController.Readout`) и в
  титульном `Readout()`. Если основательница имела в виду и его — это отдельная
  строчка, а не догадка.

#### 3. Со всех экранов обучения убраны стрелки и кнопки (дословно: «убрать стрелки все с экранов обучений. оставить только плашки с нашим текстом последним, остальные элементы подсказок убрать»)

Бит обучения рисует ОДНУ тёмную плашку с одной фразой. Убрано: капс-кнопки
`НАВОДИ · КРУТИ РУЧКУ · ТАЩИ`, все бирюзовые стрелки, включая качающуюся
дуговую стрелку отгона к пульту с датчиками. Переименование кнопки «КРУТИ
РУЧКУ» из заказа №2 отменено самой основательницей — кнопка удалена целиком.

- Удалён `View/ButtonHint.cs` (+`.meta`): в игре он был единственным, кто
  рисовал кнопку и стрелку. `View/HintArrow.cs` ОСТАВЛЕН — им пользуется
  `HintCard` грейбокс-стенда, а стенд — это рig для правил, а не экран обучения.
- `View/LevelView.cs:133` — вместо `Hint`/`SecondHint` пустой **`BeatAnchor`**:
  mount под будущие анимации Кати, ничего не рисует, ездит на то, о чём бит
  (первая деталь → середина нити → пульт с датчиками `(960,1046)` → сосуд).
  Тот же приём, что `TitleScreen.StartAnchor` после снятой кнопки НАЧАТЬ.
- `Game/LevelScreen.cs` — вырезаны `ShowBeatHint`, `DragHintSpot`,
  `AimTheSwipeArrow`, `SwipeArrowAnchor`, `AddSwipeArrowCorridor` и все их
  константы; `AimTheHints` теперь возит только якорь. `SensorsCue` сохранён —
  место осталось местом, даже когда стрелку с него сняли.
- `View/HintPlacement.cs` — вместе с картинками ушла половина класса:
  `BesideTheThread` (кнопка «ТАЩИ» вдоль нити, вне кольца прогресса),
  `ArrowCurve`/`ArrowIsClear`/`EdgeTowards` (выборка изгиба стрелки),
  `ArrowDetourBudget`, `OverlapArea`. `Beside` без `arrowBlockers` ведёт себя
  ПОБАЙТОВО так же, как раньше вела с `null` — игра и звала её только с `null`.
- `View/ArtScreens.cs:32` — ключи трёх кнопок убраны; PNG остаются в Resources
  (нарисованный и оплаченный ассет не удаляют из-за смены решения о том, где он
  стоит), просто игра их больше не называет.
- `View/HintPlate.cs:56` — `PlateHeight` стал собственной константой 88 px
  (было `ButtonHint.ButtonHeight`), с записью, откуда это число.

**Побочная находка, найденная ГЛАЗАМИ на пересъёмке, а не тестом.** На первом
же кадре 05 после снятия стрелок плашка отгона легла на три пипса кота. Причина:
пипсы висят НИЖЕ прямоугольника мысли, и единственным, что раньше отталкивало
плашку от них, был коридор стрелки, который плашка обязана была обходить.
Стрелка ушла — измерение осталось: `LevelScreen.PaintedRectOf` (прямоугольник
мысли, дотянутый вниз до `ArtThoughtView.DrawnBottomY`), гард в
`TheSwipeBeat_SaysWhatToDo_ClearOfTheArt`.

**Тесты (PlayMode).** Новый `TheTutorial_DrawsNothingButItsPlates` — негативная
половина заказа, и та, что гниёт молча: проверяется СЦЕНА, а не удалённые
классы. На каждом из четырёх битов на слое подсказок не должно быть ни одного
`Graphic`, кроме плашки и её собственных детей, а на `BeatAnchor` — ни одного
вообще (якорь, тихо отрастивший Image, — это вернувшаяся кнопка). Переведены:
`TheTeachingPlates_CoverNoDetail_NoVessel_AndNoHudWidget` (гард Б-1 «деталь
видна» — с кнопки на плашку, как и просили),
`TheSwipeBeat_SaysWhatToDo_ClearOfTheArt`,
`NoTeachingPlate_CoversADetail_TheVessel_OrTheHud` и
`EveryTeachingPlate_StaysWithinReachOfWhatItTeaches` (EditMode — теперь размеряют
плашку по строке реестра, на всех пяти уровнях). Честно удалены два теста
вместе с тем, что они мерили: `TheSwipeArrow_ClearsTheWholeThought_…` и
`TheDragButton_FollowsTheMovingDetail_…`.

**Канон догнан supersede-записями:** MECHANICS §2 (правило взгляда — полностью
новый блок с арифметикой самолёта), MECHANICS §5 (подсказки обучения + имя
органа пульта), SCREENS §Обучение (убранные элементы, сохранённые якоря,
таблица фраз с «крутилкой», переписанный список битов),
`gameplay-walkthrough.html` (подписи кадров 1/4/14/17 и строка реестра
«Выведено 2026-09-22»).

### Доводка трёх правок (сессия после посадки f0b84bd)

Посадка `f0b84bd` была сделана по слову основательницы «пуш что есть» —
предыдущего владельца остановили на середине. Эта сессия принимала работу и
закрывала хвосты. **Ничего не коммичено.**

**Что уже было сделано (проверено чтением кода, не переделывалось).** Все три
заказа оказались доведены до контракта, а не брошены на середине:

1. Захват по площади — `Mechanics/GazeSelector.cs:80` (параметр `shapes`,
   лексикографический выбор «расстояние до прямоугольника, затем до якоря»),
   `GazeSelector.SqrDistanceTo:150`, прокидка габаритов
   `Game/LevelScreen.cs:167-184` → `Stand/CollectionRuntime.cs:92`, вызов
   `CollectionRuntime.cs:248`. Тесты на месте и по делу:
   `Tests/EditMode/LevelAndGazeTests.cs:102` (оба конца реального самолёта +
   негативный контроль на том же расстоянии от якоря), `:146` (выдержка на
   кончике), `:177` (спор двух наложенных).
2. «Крутилка» — `Game/GameTexts.cs:61` и `:94` живые, старые формулировки в
   `Withdrawn:196-197`, стенд и легенда пульта переведены. Грепом по всем
   строковым литералам `Assets/_Project`: живых «ручка» не осталось ни одной
   (остались только элементы `Withdrawn` и текст assert'ов, что и требуется).
3. Стрелки — `View/ButtonHint.cs` удалён, `LevelView.BeatAnchor:153` пустой
   mount под анимации Кати, гард `TheTutorial_DrawsNothingButItsPlates`
   (`Tests/PlayMode/GameFlowTests.cs:832`) проверяет СЦЕНУ. `View/HintArrow.cs`
   жив только под `HintCard` грейбокс-стенда — это его родные стрелки.
   Гард Б-1 «деталь видна» перевешен на плашку (`GameFlowTests.cs:795`).

**Что доделано в эту сессию.**

- **Розовый квадрат — баг реальный, найден и починен.** Симптом рисуется
  намеренно: `View/LevelView.cs:935` красит ячейку добычи в мадженту, когда
  иконка пришла null. Вопрос был не «почему маджента», а «почему null у
  спрайта, который лежит на диске», и ответ в КЭШЕ, а не в ассете.
  `View/ArtLibrary.cs` спрашивал словарь только «есть ли ключ», а уничтоженный
  `UnityEngine.Object` — это одновременно присутствующий ключ и null-значение
  (живая C#-обёртка вокруг мёртвого нативного указателя). Кэш отдавал труп,
  вызывающий сравнивал с null уже перегруженным оператором и красил розовым.
  Умирают именно `Sprite.Create`-записи (вырезки иконок, запечённый сосуд У3):
  они не принадлежат ни сцене, ни файлу ассета, а лаунчер уровней со стенда
  делает `SceneManager.LoadScene` в single-режиме, за которым идёт
  `Resources.UnloadUnusedAssets`. В batchmode это невоспроизводимо — прогон
  грузит арт один раз и стреляет, поэтому маджента-скан по кадрам контракта и
  был чист при грязной живой сессии.
  Починка — `View/ArtLibrary.cs:136` `Alive<T>`: попадание в кэш проверяется на
  живость, мёртвая запись считается промахом и ключ грузится заново. Применено
  ко всем четырём точкам кэша (`Get`, `IconOf`, `VesselOf`, `Clip`). Заодно
  лечится отравление кэша: раньше одна неудачная загрузка кэшировалась как null
  на всю сессию, теперь повтор возможен, а `Reported` по-прежнему держит лог
  «один раз на ключ».
- **Гард проверен на красном, а не написан на зелёном.**
  `Tests/EditMode/LevelCatalogTests.cs:957`
  `TheArtCache_HandsBackALiveSprite_AfterACachedOneIsUnloaded` прогнан против
  СТАРОГО кода и падает ровно с симптомом основательницы: «самолёт в небе: кэш
  вернул выгруженный спрайт — в сосуде будет розовый квадрат». С починкой —
  зелёный. Убивается только вырезка (рантайм-объект), без
  `allowDestroyingAssets`, так что на диске ничего не трогается.
  `Tests/PlayMode/GamePictureTests.cs:163`
  `TheHaul_IsTheDetailItself_AndNeverAMagentaSquare` — картинка, которую видела
  основательница: три детали собраны обычным ходом беты, у каждой ячейки живой
  спрайт с живой текстурой и не маджента; оттуда же новый кадр
  `Game41_L1_haul_in_the_vessel` (крупно сосуд с добычей).
- **Кадр 35 — постановка проверена и описана честно**
  (`Tests/PlayMode/GameScreenshotTests.cs:1610`). «Пустых мест» на плите стало
  МЕНЬШЕ: правило ловит по прямоугольнику, а не по якорю. Подгонять ничего не
  пришлось, и важно почему — `BrightestOpenSpot` отбраковывает пятно, чей
  ОПИСАННЫЙ КВАДРАТ 2r пересекает прямоугольник любой детали, а квадрат,
  промахнувшийся мимо коробки, гарантирует промах и вписанного круга. Это
  строго сильнее нового правила, а не приближение старого. Страхует
  `Assert.Less(NoticedIndex, 0)` — он читается с РАБОТАЮЩЕГО селектора, так что
  съеденное последнее свободное место дало бы красный тест, а не тихо другую
  картинку.
- **Мусор убран.** `InitTestScene3b6b20ec-*.unity(.meta)` и
  `InitTestScene4361a96f-*.unity(.meta)` случайно сели в `f0b84bd`. Удаление
  зафиксировано в индексе; файлы вынесены из дерева, не стёрты.

**Прогон.** `.studio/runs/suite-20260922-190943` — честный **exit 0**, свежий
каталог, оба XML переписаны этим прогоном (EditMode 19:10:03, PlayMode 19:21:14):
**EditMode 148/148, PlayMode 157/157.** Против базы `f0b84bd` — +1 EditMode
(`TheArtCache_…`) и +1 PlayMode (`TheHaul_…`).

Два прогона до него в отчёт не идут и почему:
`suite-20260922-185220` убит SIGTERM (exit 143) — я правил исходник, пока он
шёл, и смешанный прогон недоказателен; `suite-20260922-185621` — красный
(exit 2) на МОЁМ новом тесте: уровень 1 открывается обучением, и второй сбор
подряд висит, пока не отвечен бит отгона. Постановка переписана по образцу
`Game06_L1_play` (собрать одну → `SwipeUntil(Beat == Done)` → остальные), запас
волн глушится только после обучения — иначе бит отгона нечем отвечать.

**Кадры.** Переснят весь контракт (56 + новый `Game41_L1_haul_in_the_vessel`).
Просмотрено глазами: `Game01_title` (кнопки нет, «Крути крутилку, чтобы начать»
+ ПК-скобка), `Game17/04/05/40` (только тёмные плашки — ни стрелок, ни кнопок,
ни пиктограмм; на 04 деталь в пути видна и не похоронена плашкой, на 05 плашка
лежит ниже трёх пипсов кота), `Game35/36` (прицел), `PreviewMenu` (легенда
пульта «колесо мыши = крутилка»), `Game41` (в ведре самолёт, чайка и кораблик —
маджентой не пахнет).

**Замечено глазами, НЕ чинилось — на решение основательницы.**
1. Кадр 35: свободное место под прицел уехало в левый верхний угол кадра. Это
   прямое следствие нового правила (ловим по прямоугольнику), тест честный, но
   как ДЕМОНСТРАЦИЯ неона угол — слабее середины. Если кадр нужен «по центру
   пустоты», это уже разговор про композицию уровня, а не про гард.
2. Титул: «Крути крутилку, чтобы начать» идёт по яблокам и кружке — слово
   «начать» ложится прямо на кружку. Читается, но не чисто. Строка и её место
   пришли из прошлых кругов, не из этих трёх правок, поэтому не трогал.

**Редактор.** На входе в сессию ни одного Unity-процесса meditation не было —
убивать и возвращать нечего; редактор остался закрытым.

### Прод-чистка: клавиатурные подсказки и вход в панель убраны из игры

Три заказа founder одного вечера, все про то, что игра едет на АВТОМАТ, а не на
стол разработчика.

**1. «Из медитации срочно убрать все подсказки про клавиатуру — она играется на
автомате».**

Под пятью строками игры стояла ПК-скобка — вторая строка, мельче и приглушённее.
Она задумывалась как сноска для игры за столом и ровно так и читалась, пока в игру
играли за столом; на стойке сноска — это второе указание, и оно неправда.

- `Scripts/Game/GameTexts.cs` — удалены константы `TitleStartOnDesk`,
  `BeatAimOnDesk`, `BeatCollectOnDesk`, `SwipeHintOnDesk`, `FinaleExitOnDesk`;
  `Live` ужался с 11 строк до 6. Четыре различных литерала (скобка про колесо
  мыши стояла и на титуле, и на бите сбора) занесены в `Withdrawn` — реестр
  выведенных обязан их знать, иначе сторож не узнает их при возврате.
- `Scripts/Game/TitleScreen.cs:93` — `DeskLabel` и его полоса (`DeskLabelY` 1032,
  `DeskLabelPt` 26) сняты; на титуле одна строка на y 983.
- `Scripts/Game/FinaleScreen.cs:35` — то же самое: `DeskLabel`, `DeskLabelY`,
  `DeskLabelPt` сняты, подпись одна.
- `Scripts/Game/LevelScreen.cs:408/427/489/597` — все четыре бита обучения зовут
  плашку без скобки; `ShowBeatSentence` потерял параметр `bracket`.
- `Scripts/View/HintPlate.cs`, `Scripts/View/LevelView.cs` — двустрочная форма
  плашки ЦЕЛА и задокументирована как неиспользуемая: убрана строка, а не
  типографский приём. Плашка сама меряется на одну строку (`SizeFor` без флага),
  поэтому она стала НИЖЕ, а не просто потеряла текст.
- **Грейбокс-стенд не тронут.** `PreviewMenuController:137` держит свою легенду
  «на ПК: стрелки = джойстик, колесо мыши = крутилка…» — это дев-инструмент,
  который открывают только за столом.

**2. «Убрать кнопку параметры! это же прод билд под автомат».**

Панель ехала свёрнутой, и в углу прод-кадра оставалась кнопка «параметры»: нажать
её на стойке нечем (мыши нет), и игрок пробует нажать именно поэтому.

- `Scripts/Game/GameFlow.cs:146` — `StandLevelLaunch.Take()` читается ДО панели, и
  `TuningPanel.Create` теперь стоит внутри ветки «открыл стенд». На пути лаунчера
  `Panel == null`: объект не создаётся вовсе. Не спрятан и не обнулён по альфе —
  нечего развернуть обратно, нечему зарезервировать полосу кадра, и
  `PanelVisible: true`, забытый в `UserSettings/tuning.json`, не может вернуть
  тюнер на автомат.
- **Инструмент цел, вход со стенда уже был.** Правая колонка меню стенда
  («Уровень N», 2026-08-19) открывает эту же сцену через `StandLevelLaunch`, и на
  этом бут-е панель строится как раньше — тот же каталог `TuningCatalog.Game()`,
  те же живые значения, тот же файл. Добавлять пункт в меню стенда не пришлось.
  Сценки стенда свои панели держат сами (`PreviewSceneController:79`).

**3. Подпись финала называла не ту кнопку.**

`GameTexts.FinaleExit` = «Жми кнопку Меню для выхода в главное меню»
(формулировка founder). Было «Красная кнопка — выход в главное меню» → в
`Withdrawn`. Это не правка тона: выход на автомате делает отдельная кнопка «Меню»
(`ArcadeInput.MenuButton` → `MenuButtonExit`), красная — другой контрол пакета, и
игра её никогда не слушала. Подпись, называющая орган, который код не слушает,
хуже отсутствия подписи: игрок жмёт, ничего не происходит, и автомат выглядит
сломанным. **Код выхода не тронут** — контракт хаба тот же.

**Тесты.**

- `Tests/EditMode/LevelCatalogTests.cs:515` — четыре обучающие плашки объявлены
  однострочными (`hasBracket: false`), то есть тесты размещения меряют ту
  плашку, которая теперь рисуется. Новых гарда два:
  `NoLiveLine_NamesAKeyboardAMouseOrAPc` (ни одна живая строка не называет
  компьютер, клавишу, мышь, колесо, Esc) и `TheWithdrawnList_KnowsThePcBrackets`
  (все четыре скобки занесены в реестр выведенных — иначе сторож их не узнает).
- `Tests/PlayMode/GameFlowTests.cs` — сторож по СЛОВАМ, а не по строкам:
  `NoKeyboardHint_IsOnAnyScreenOfTheGame` обходит титул → карточку → все четыре
  бита обучения → финал и падает на любом `Text`, где встретилось слово из
  списка. Верный список ловит четыре вчерашние скобки; список по словам ловит
  ещё и завтрашнюю пятую. Проверен на красном:
  `TheKeyboardGuard_CatchesABracketPutBackByHand` возвращает скобку на титул
  руками и требует, чтобы сторож упал.
- Переведены честно, а не ослаблены: `TheTitle_SaysHowToStart_…` больше не
  сверяет `DeskLabel`, а требует ОТСУТСТВИЯ клавиатурных слов на экране;
  `TheSwipeBeat_…` и `TheSwipeHint_IsTheDropsPlate_…` вместо «скобка мельче и
  приглушённее» требуют, чтобы объект второй строки был выключен, строка пуста,
  и — негативный контроль — чтобы плашка без скобки была НИЖЕ плашки со скобкой
  (иначе строку сняли бы только с глаз).
- `TheShippedGame_HasNoTuningPanelAtAll` — заявка ОТСУТСТВИЯ, сделанная тремя
  способами (нет `GameFlow.Panel`, нет компонента `TuningPanel` в сцене, нет
  объектов `TuningPanel`/`Collapse` в кадре) плюс `Stage.ReservedRight == 0`.
  Прогоняется поверх `PanelVisible = true` в конфиге — автомат не должен быть в
  одной строке JSON от тюнера.
- `ThePanel_IsStillThere_WhenTheStandOpensTheGame` — негативный контроль к нему:
  та же сцена, открытая через `StandLevelLaunch.Open` (настоящий путь founder),
  панель на месте, кнопка в кадре, тело разворачивается и в нём есть ручки.
  Хелперы `GameTestHarness.LoadGameAsTheCabinetDoes` /
  `LoadGameFromTheStand` разделяют два бута одной сцены.
- `TheFinale_NamesTheButtonThatActuallyLeaves` — подпись из реестра, содержит
  «Меню», НЕ содержит «красн», не совпадает ни с одной выведенной строкой.
- `ThePanelIsOnEveryLevel_…` переименован в
  `TheTunedValuesSurviveTheWholeRun_WithNoPanelInTheGame`: тяжёлая половина
  (значения уровня переживают прогон) осталась, виджет ушёл.

**Прогон — ПОСАЖЕНО ПО СЛОВУ FOUNDER ДО ПОЛНОГО ПРОГОНА.** Полная сюита этим
инкрементом НЕ зелёная-подтверждённая, и это сознательно: founder попросила
посадить срочно и прогнать сюиту самой. Что есть:

- **EditMode — честный зелёный на ЭТОМ дереве:** `.studio/test-editmode.xml`,
  `150/150`, `result="Passed"`, 2026-09-22 17:22:01Z → 17:22:04Z
  (`.studio/runs/suite-20260922-202147`). В нём оба новых гарда текстов. Это же
  и доказательство КОМПИЛЯЦИИ обеих сборок: `grep -c 'error CS'` по логу
  прогона = 0, новых warning CS в `Assets/_Project` нет.
- **PlayMode — прогнан на этом же коде в предыдущем прогоне**
  (`.studio/runs/suite-20260922-200811`): `159/160`. Единственный красный —
  `GamePictureTests.EveryLevelsThoughts_AreBlackInk_AndTheHaloIsAToggle(4)`
  («Город»): подложка мысли вышла в 1.71× ярче подложенного при пороге 2×. К
  этим правкам отношения не имеет и является ФЛЕЙКОМ композиции: тест меряет
  пиксели шести дрейфующих мыслей на пятом уровне, тот же код дал зелёный
  `160/160` прогоном ранее (`suite-20260922-195542`), а геометрия кадра от
  снятой панели не зависит — `DesignStage.Fit()` зовётся и в `Create`, и каждый
  `LateUpdate`, а панель и до правки ехала свёрнутой (`ReservedRight = 0`).
  **Флейк оставлен как есть — не мой инкремент и не мой вызов его «чинить».**

**Кадры.** Свежие, сняты прогоном `suite-20260922-200811` с ЭТОГО кода
(20:16–20:19). Просмотрены глазами:
- `Game01_title` — одна строка «Крути крутилку, чтобы начать», скобки про колесо
  мыши нет;
- `Game17/04/05/40` — все четыре плашки обучения однострочные по смыслу (текст
  переносится, второй строки-сноски нет); на 05 плашка по-прежнему ниже трёх
  пипсов кота, на 04 деталь в пути не похоронена;
- `Game13_finale` — «Жми кнопку Меню для выхода в главное меню», скобки про Esc
  нет;
- `PreviewMenu` — стенд не менялся, его ПК-легенда на месте (так и задумано).

Оговорка про кадры честная: скриншоты снимаются с `TuningPanel.ScreenshotMode =
true`, поэтому кнопки «параметры» на них не было и ДО правки — отсутствие входа
в панель доказывают тесты, а не картинка.
