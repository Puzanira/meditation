# Increment: запуск уровней по отдельности + хвост круга 2026-08-08

Дата: 2026-08-19. Статус: гейт 1 пройден («ок» на пересказ), владелец запущен.

## Состав

1. **Запуск уровней сценками (заказ founder: «надо запускать уровни сценками
   по отдельности — для дебага и разработки»).** На грейбокс-стенде к четырём
   сценкам добавляются пять пунктов «Уровень 1…5»: запуск НАСТОЯЩЕГО уровня
   (арт, правила, три контроллера) напрямую — без титула, карточки и обучения,
   сразу игра. Esc (MenuButton) — назад в меню стенда, не в титул игры.
   Тюнинг-панель как в игре: полоса активного уровня подхватывается
   (ApplyLevel), правки живут сразу.
2. **Хвост круга 2026-08-08** (вердикты в
   `docs/increments/2026-08-08-founder-live-feedback.md`):
   - доделать уборку слотов грейбокс-стенда (WIP в дереве: StageView,
     StandBootTests, ScenetteGameplayTests — начато, проверить и довести);
   - **Б1**: карточку «Маши над датчиком!» перевести на стиль трёх обучающих
     карточек (ButtonHint: тёмная плашка RGB 35,52,65, белый капс, рамка 2 px)
     вместо белой грейбокс-бумаги HintCard;
   - **Б2**: полоса наполнения (весь HUD) выше слоя мыслей по Z — SCREENS:45
     «HUD они не закрывают»; кадры Game23/Game15 показывали полосу под
     кольцами пипсов (23.2 % / 10.4 %);
   - doc drift: SCREENS:168 «слот в HUD заполняется», ARCHITECTURE:224/365
     про слоты, MECHANICS:232 амплитуда 0.2 → 0.08 (+кулдаун 45 мс);
   - чекпойнт-лог 2026-08-08 про Game09 «полоса заполнена частично» — неверен
     (0 %), поправить формулировку.

## Approved by founder

2026-08-19, терминал: «ок» после пересказа (пять пунктов уровней на стенде +
хвост круга одним заходом).

## Done contract

1. player path (дебаг): из меню стенда открывается любой из пяти уровней,
   играется тремя контроллерами, Esc возвращает в стенд; из стенда в игру
   и обратно — без грязного состояния (рестарт уровня чистый).
2. Слотов-миниатюр нет ни в игре, ни на стенде; карточка отгона в стиле
   остальных подсказок; полоса наполнения видима поверх мыслей на пике
   (пиксельный гард).
3. Тесты: стенд-уборка зелёная; новые пункты меню покрыты (загрузка каждого
   уровня из стенда, возврат по MenuButton); гарды Б1/Б2 меряют пиксели.
4. hygiene: SCREENS/MECHANICS/ARCHITECTURE догнаны; тексты через GameTexts.
5. regressions: suite GREEN честный exit 0, свежий каталог runs; кадры
   контракта, затронутые Б1/Б2 и стендом, пересняты.

## Checkpoint log

- 2026-08-19: файл создан, владелец запущен.
- 2026-09-22, владелец (одна сессия, вместе с
  `2026-09-22-playtest-feedback-big-list.md`). **Состав закрыт полностью.**

  **(А) WIP из дерева компилируется.** Прошлого владельца остановили на середине,
  но код собрался с первого раза: три сборки (`Meditation`,
  `Meditation.Tests.EditMode`, `Meditation.Tests.PlayMode`), ноль CS-ошибок.
  Незакрытым в WIP оставалось только то, что перечислено ниже.

  **(1) Запуск уровней сценками по отдельности — ЗАКРЫТО.**
  - `Stand/StandLevelLaunch.cs` (новый) — статическая заявка «открой уровень N»,
    которая ПОТРЕБЛЯЕТСЯ (`Take()`), поэтому ровно один буст игры на неё
    реагирует; лаунчер стойки открывает ту же сцену без заявки и получает титул.
  - `Stand/PreviewScenes.cs` — `LevelCount`/`LevelTitle`/`LevelDescription`,
    имена берутся из `LevelCatalog`, а не из `GameTexts` (стенд — прибор, а не
    игра, говорящая с игроком; «Уровень N» в реестре числится выведенным).
  - `Stand/PreviewMenuController.cs` — две колонки: слева четыре сценки, справа
    пять уровней; джойстик ↑↓ идёт сквозь всё меню, ←→ прыгает между колонками.
  - `Game/GameFlow.cs` — `LaunchedFromStand`/`StandLevelIndex`, вход сразу в
    `GamePhase.Level`, `NoteTutorialGiven()` (без обучения), и обе концовки
    уровня ведут обратно в меню стенда (`ExitToStand`). `ApplyLevel` работает
    как в игре, поэтому уровень со стенда — тот же уровень, а не похожий.
  - **Тесты, которых в WIP не было, дописаны:**
    `StandBootTests.Menu_AlsoListsTheFiveLevels_AndTheStickCrossesBetweenTheColumns`
    и `Menu_OpensARealLevel_AndTheMenuButtonComesBackToTheStand` ([Values 0,2,4]) —
    открытие настоящего уровня, его арт, его полоса тюнинга, отсутствие обучения,
    возврат по MenuButton и то, что заявка не остаётся висеть.

  **(2) Б1 — карточка отгона в стиле дропа: ЗАКРЫТО** (было в WIP, доведено).
  `View/HintPlate.cs` — тёмная плашка RGB(35,52,65), белый текст, рамка 2 px,
  прямые углы. В этой же сессии выросла: несёт все четыре предложения обучения
  нового списка, с переносом строки и ПК-скобкой. Гарды:
  `GameFlowTests.TheSwipeHint_IsTheDropsPlate_NotTheStandsWhitePaper`
  (конструкция) + `GameScreenshotTests.AssertTheSwipePlateIsDark` (пиксели кадра
  Game05, медиана светимости < 90/255 против 253.6 у белой бумаги).

  **(3) Б2 — полоса наполнения поверх мыслей: ЗАКРЫТО** (было в WIP).
  Дело было не в Z-порядке (полоса на HUD-слое с того же гейта), а в
  ПРОЗРАЧНОСТИ: трек 22 %-чёрного с 32 %-белой рамкой пропускал штриховку
  насквозь (23.2 % пикселей на Game23, 10.4 % на Game15). Теперь трек, рамка и
  заливка непрозрачны. Гард с НЕГАТИВНЫМ контролем:
  `GamePictureTests.TheFillBar_IsOpaqueOverTheThoughts_AndTheGuardCatchesItWhenItIsNot`
  — тот же замер с HUD, опущенным под слой мыслей, обязан уйти за потолок.
  Заодно проверено, что правка пережила новый дефолт «мысли закрывают сосуд»
  (`ThoughtsCoverVessel = true`): полоса живёт на HUD, который выше мыслей при
  обоих порядках слоёв.

  **(4) Уборка слотов грейбокс-стенда — ЗАКРЫТО, кода там уже не было.**
  Проверено грепом: `StageView` слотов не строит, в `Scripts/` и `Tests/` от
  ряда остались только комментарии-надгробия. Поправлен ДРИФТ ДОКИ (ниже).

  **(5) Doc drift — ЗАКРЫТО:**
  - `docs/design/SCREENS.md` «Сбор (визуал состояния)»: ~~«слот в HUD
    заполняется»~~ → «полоса наполнения под сосудом растёт».
  - `components/unity-game/docs/ARCHITECTURE.md` (не в `docs/`!) — строка про
    `StageView`: убраны «слоты» из списка состояний и вычеркнуто «HUD одинаков
    во всех сценках: слоты есть всегда»; строка про S4 догнана наездом на
    ведёрко и рябью.
  - `docs/design/MECHANICS.md` итоги плейтеста: амплитуда удара **0.2 → 0.08**,
    добавлен пропущенный **кулдаун 45 мс**, дата перенастройки 2026-08-07 →
    2026-08-08.
  - `docs/increments/2026-08-08-founder-live-feedback.md` — чекпойнт про
    `Game09_L2_play` «полоса заполнена частично» **поправлен**: там 0 % (полоса
    серая); 12.2 % — на Game05/06/07/27/29.

  Done contract §1–§5 выполнен. Сюита и кадры общие с большим списком того же
  дня: `studio-go suite --project meditation`, честный **exit 0**, свежий
  каталог `.studio/runs/suite-20260922-150215/`, **EditMode 139/139**,
  **PlayMode 156/156**; скриншот-контракт переснят целиком. Подробности и
  просмотренные глазами кадры — чекпойнт в
  `2026-09-22-playtest-feedback-big-list.md`. Ничего не закоммичено.
