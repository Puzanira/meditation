# «Медитация в спешке» — архитектура компонента

Unity 6000.5.3f1, URP 2D. В сборке два продукта, и это осознанно:

- **игра** — уровни 1–5 с артом дизайнера и полным потоком экранов S1–S6.
  Entry-сцена автомата, всё, что увидит игрок;
- **превью-стенд** — меню и четыре сценки, в которых механики видно без
  картинки. Он остался после того, как игра появилась: судить кручение и
  отгон по greybox-прямоугольнику проще, чем поверх фотографии, и его
  тесты — единственное место, где правила проверяются в изоляции.

Под ними ОДИН слой правил: `CollectionRuntime`, `LevelRules`, `ThoughtField`,
`CrankCollector`, `GazeSelector`. Обе картинки реализуют `ICollectionView`,
поэтому код, который пинают тесты стенда, — это код, который играет игра.

Что выведено на панель каждой сценки и на панель игры — зафиксировано тестом
`TuningPanelCoverageTests`, он же единственное место, где это решение записано.

Источники истины: `docs/design/SCREENS.md` (координаты),
`docs/design/gameplay-walkthrough.html` (как это выглядит покадрово, тексты),
`docs/design/MECHANICS.md` (правила и параметры),
`system/specs/ARCADE_INTEGRATION_CONTRACT.md` (граница интеграции в автомат).

---

## 1. Топология сцен

| Сцена | Файл | Что внутри |
|-------|------|------------|
| **Игра (entry)** | `Assets/_Project/Scenes/Game.unity` | `GameFlow` |
| Меню стенда | `Assets/_Project/Scenes/PreviewMenu.unity` | `PreviewMenuController` |
| Сценка 1 «Динамо-сбор» | `Scene1_CrankCollect.unity` | `Scene1CrankCollect` |
| Сценка 2 «Отгон взмахами» | `Scene2_ShakeAway.unity` | `Scene2ShakeAway` |
| Сценка 3 «Две руки» | `Scene3_TwoHands.unity` | `Scene3TwoHands` |
| Сценка 4 «Уровень целиком» | `Scene4_FullLevel.unity` | `Scene4FullLevel` |

Все шесть — в Build Settings, **`Game` первая**: entry-сцену открывает лаунчер
(ARCADE_INTEGRATION_CONTRACT §5–§6), и «в меню» из любого экрана игры — это
запрос выхода лаунчеру (§3 «Кнопка „в меню“»), а не перезагрузка `Game.unity`.
Каждая сцена содержит ровно четыре объекта:

```
Main Camera        — ортокамера, чистит фон (весь UI — Screen Space Overlay)
EventSystem        — + InputSystemUIInputModule (мышь для панели тюнеров)
Stand
  ├─ ArcadeInputRunner   (пакет arcade-controls: качает ArcadeInput каждый кадр)
  ├─ <SceneController>   (правила сценки)
  └─ MenuButtonExit      (контракт «в меню»)
```

**Сцены строятся кодом:** `Assets/_Project/Editor/GameSceneBuilder.cs`, меню
`Meditation → Rebuild All Scenes`. Один сборщик на все шесть сцен — сборщик,
знающий только про стенд, молча переписывал бы Build Settings без `Game`;
`PreviewStandSceneBuilder` остался форвардером ради старого пункта меню и
записанной в этом доке headless-команды. Весь экран собирается процедурно в
рантайме, поэтому в `.unity` нет вёрстки — композиция целиком живёт в читаемом
коде (`StageView` / `LevelView`), а сцены воспроизводимы одной командой.

## 2. Слои кода

```
Assets/_Project/
├─ Art/Resources/MeditationArt/   арт-дроп:
│    L1…L5/            background + objects/ + thoughts/
│    screens/          готовые экраны: title, level-N-card, level-complete, game-over, finale
│    buttons/          start, navodi, kruti-ruchku, tashi
│    audio/            level-1…level-5, meditation, thoughts (MECHANICS §8)
│    shaders/          thought-fade, thought-backing, detail-sweep
├─ Scripts/            (asmdef Meditation; namespace Meditation.*)
│  ├─ Mechanics/       чистая логика без Unity-объектов → EditMode-тесты
│  ├─ View/            UGUI-примитивы, greybox-композиция стенда и арт-композиция уровня
│  ├─ Tuning/          статический конфиг параметров + экранная панель
│  ├─ Game/            поток экранов S1–S6, уровень, обучение, реестр выведенных текстов
│  ├─ Stand/           навигация стенда, базовый контроллер сценки, кнопка «в меню»
│  └─ Scenes/          четыре контроллера сценок
├─ Editor/             (asmdef Meditation.Editor) сборщик сцен + правила импорта арта и звука
└─ Tests/EditMode|PlayMode
```

### Game — поток экранов (SCREENS.md S1–S6)

`GameFlow` — единственный контроллер сцены игры и её машина состояний. Экраны
не отдельные сцены, а слои кадра: каждый владеет своим `Ui.Layer` и уничтожается
целиком, когда поток идёт дальше. Поэтому «в меню» нечего разматывать, и экран
физически не может утащить свой таймер в следующий. (Своих часов у уровня с
2026-08-07 нет, но правило про изолированные экраны от этого только вернее.)

**Одна сцена — два продукта (срочная правка founder 2026-09-22).** `Game.unity`
загружают двое: лаунчер автомата (ничего не «заявлено», поток открывается на титуле)
и правая колонка стенда — «Уровень N» через `StandLevelLaunch`. На ПЕРВОМ пути
панели тюнинга больше НЕТ: `GameFlow.Panel` строится только когда заявка стенда
непуста. «Убрать кнопку параметры! это же прод билд под автомат» — панель ехала
свёрнутой, и в углу прод-кадра оставалась кнопка «параметры», которую нечем нажать
(мыши на стойке нет) и которую игрок пробует нажать именно поэтому. Не спрятана и не
обнулена по альфе — объект не создаётся вовсе: нечего развернуть обратно, нечему
зарезервировать полосу кадра, и `PanelVisible: true`, забытый в сохранённом
`tuning.json`, не может вернуть тюнер на автомат. Инструмент при этом цел: со стенда
панель та же, с тем же каталогом и тем же файлом, — основательница тюнит настоящий
уровень оттуда. Пара тестов держит обе стороны:
`TheShippedGame_HasNoTuningPanelAtAll` и `ThePanel_IsStillThere_WhenTheStandOpensTheGame`.

| Класс | Экран | Чем кончается |
|-------|-------|----------------|
| `TitleScreen` | S1 титул — готовый рендер + живая строка «Крути крутилку, чтобы начать» и полоса оборотов; на месте кнопки — пустой `StartAnchor` под анимацию Кати | 2 оборота динамо (720°) |
| `LevelCardScreen` | S2 карточка — готовый рендер уровня | 4 с, авто **[tune 1.5–8]** |
| `LevelScreen` | S3 уровень + S4 победа + S5 поражение | победа → следующая карточка / финал; поражение → карточка того же уровня |

| `FinaleScreen` | S6 финал — готовый рендер-панорама + подпись «Жми кнопку Меню для выхода в главное меню» | любой ввод или 20 с тишины → выход |

**Все экраны, кроме игрового, — картинки дропа 2026-08-07** (`screens/…`, рендеры
3840×2160 → даунскейл ×0.5 на импорте). Тексты запечены в них, поэтому `GameTexts`
из «реестра строк» превратился в **список выведенных из игры строк**, а PlayMode-тест
`NoWithdrawnLine_IsRenderedOnAnyScreenOfTheFlow` обходит поток и падает, если хоть
одна из них снова рисуется поверх готового экрана. Инверсия намеренная: строка не
исчезает от того, что её удалили с одного экрана, — она возвращается в следующий
раз, когда кому-то понадобится «подпись вот сюда», в шрифте, которого дизайнер не
выбирала, поверх картинки, где это уже написано.

> **SUPERSEDED (плейтест founder 2026-08-08, дополнено 2026-09-22): инверсия НЕ
> абсолютная.** Здесь стояло «игра не рисует НИ ОДНОЙ строки» — это было верно ровно
> один день, пока основательница не села за автомат и не смогла начать. `GameTexts`
> снова реестр В ОБЕ стороны: `Withdrawn` — чего рисовать нельзя, `Live` — что можно,
> и PlayMode-тесты обходят поток по обоим спискам. Сейчас игра рисует сама: строку
> титула «Крути крутилку, чтобы начать», четыре текста обучения и подпись выхода на
> финале — шесть строк, все однострочные. Правило, которое осталось: не перенабирать
> руками то, что дизайнер уже запёк в картинку.
>
> **Клавиатурные подсказки выведены (срочная правка founder 2026-09-22):** «из
> медитации срочно убрать все подсказки про клавиатуру — она играется на автомате».
> Под пятью строками стояла ПК-скобка («на компьютере — колесо мыши / стрелки /
> Q и A / Esc»); все пять — в `Withdrawn`, и сторож `NoKeyboardHint_IsOnAnyScreenOfTheGame`
> ловит уже не сами строки, а СЛОВА (компьютер, клавиша, мышь, колесо, Esc) на любом
> экране игры: верный список ловит пять вчерашних скобок, а не завтрашнюю шестую.
> Грейбокс-стенд не тронут — его открывают только за столом.

Переходы — фейд в чёрный 0.3 с: подмена экрана происходит в самой тёмной точке,
поэтому ни один кадр не показывает два экрана сразу.

`LevelScreen` держит машину состояний уровня (`intro` 2 с → `play` → `win`/`lose`;
«пик хаоса» — это вид, не состояние) и три скриптовых бита обучения уровня 1.

**Обучение помнит ПОТОК, а не экран** (решение founder 2026-08-07: «обучение не
повторяется после поражения»). Экран уровня строится заново на каждую попытку и
поэтому не может помнить ничего — ровно поэтому обучение и переигрывалось. Флаг
`GameFlow.TutorialAlreadyGiven` ставится при ВХОДЕ в уровень 1, а не по окончании
битов: проигравший во время обучения всё равно уже обучен. Сбрасывается он на
титуле, а не в `StartRun()`, потому что на титул ведут все три дороги (финал, «в
меню», фолбэк лаунчера), а `StartRun` — только одна: у автомата за игрой садится
следующий человек, и флаг, переживший прогон, показал бы ему игру, молча
считающую, что он смотрел чужое обучение. Рестарт уровня 1 пропускает и обзор
(`LevelScreen.SkipsTheIntro`): «сразу в игру» — это про обе части.

> ### SUPERSEDED 2026-09-22 — на экранах обучения остались ТОЛЬКО плашки
>
> Заказ основательницы, дословно: «убрать стрелки все с экранов обучений.
> оставить только плашки с нашим текстом последним, остальные элементы
> подсказок убрать».
>
> Всё, что ниже в этом разделе описывает **картинки-кнопки** (НАВОДИ · КРУТИ
> РУЧКУ · ТАЩИ), **стрелки** (включая качающуюся стрелку отгона к пульту) и их
> размещение (`ButtonHint`, `HintPlacement.BesideTheThread`, коридор штриха,
> «стрелка вторым критерием») — **история**. Класс `ButtonHint` удалён,
> половина `HintPlacement` вместе с ним; `HintArrow` остался ТОЛЬКО стенду
> (`HintCard`). Причина — бит говорил одно и то же трижды: нарисованным словом,
> стрелкой и предложением, тремя разными языками поверх фотографической плиты.
>
> Что действует сейчас: бит рисует **одну тёмную `HintPlate` с одной фразой из
> `GameTexts`**, и больше ничего. Место ей по-прежнему ищет
> `HintPlacement.Beside` против `LevelCatalog.HintObstaclesOf` (+ траектория
> едущей детали на бите сбора, + всё, что РИСУЕТ обучающая мысль, на бите
> отгона — `LevelScreen.PaintedRectOf`: пипсы висят ниже прямоугольника, и
> раньше от них отталкивал только коридор стрелки).
>
> Вместо кнопок — пустой **`LevelView.BeatAnchor`**: mount под анимации Кати,
> ездит на то, о чём бит (первая деталь → середина нити → `SensorsCue` → сосуд),
> ничего не рисует. Тот же приём, что `TitleScreen.StartAnchor`. Держат это
> `GameFlowTests.TheTutorial_DrawsNothingButItsPlates` (на слое подсказок нет ни
> одного `Graphic`, кроме плашки; на якоре — ни одного вообще),
> `…TheTeachingPlates_CoverNoDetail…` и арифметика каталога
> `LevelCatalogTests.NoTeachingPlate_Covers…` / `EveryTeachingPlate_StaysWithinReach…`.
>
> Имя органа пульта в живых строках — **«крутилка»**, не «ручка»
> (`system/CONTROLS_BRIEF.md`); старые формулировки — в `GameTexts.Withdrawn`.

**Обучение с дропа 2026-08-07 — картинки-кнопки, и порядок битов другой:**
НАВОДИ → (КРУТИ РУЧКУ + ТАЩИ) → бит отгона. Прежний первый бит замечал деталь ЗА
игрока и просил только ручку, поэтому взгляд — штатный способ выбора детали —
объяснялся последним, уже после обучения. Теперь он объясняется первым, той самой
кнопкой, которая про него и есть. Второй бит показывает ДВЕ кнопки сразу (SCREENS
§Обучение п.2): «КРУТИ РУЧКУ» у индикатора динамо и «ТАЩИ» у едущей детали — одна
называет руку, вторая то, что рука делает. У бита отгона кнопки нет: в дропе нет
«ТРЯСИ» (вопрос у дизайнера), поэтому это стрелка на ПУЛЬТ С ДАТЧИКАМИ без текста,
качающаяся тем самым жестом (`ButtonHint.ShowArrowOnly`). Взмах над датчиком высоты —
движение вертикальное, поэтому и стрелка качается ВДОЛЬ себя (кончик ходит вверх-вниз,
`SwipeWobblePx` 60 при длине 240): боковой замах, который она рисовала до 2026-08-07, —
это картинка трясущегося джойстика, а джойстик с этого дня только целится.

Куда садится обучающая мысль, решает уровень, а не каталог: раньше это был
`Details[1]`, и работало только потому, что второй объект той сцены случайно стоял
посреди кадра. После перенумерации выбирается первая несобранная деталь, над которой
слабая мысль помещается ЦЕЛИКОМ.

**Где стоят плашки — не смещение, а поиск** (`HintPlacement`, фикс-раунд
дизайн-гейта 2026-08-07). Смещение («на 230 px выше индикатора динамо») — это
догадка о картинке, которой оно не видит: на набережной плашка «КРУТИ РУЧКУ»
(468×88) после клампа к краю кадра ложилась на весь акулий плавник и на левый край
ведёрка. Приёмка п.3 («ни один виджет не перекрывает деталь или сосуд») распространена
гейтом на обучающие плашки — подсказка, закрывающая то, про что уровень, учит при
выключенном уровне. Поэтому позиция ищется: `LevelCatalog.HintObstaclesOf` отдаёт все
прямоугольники уровня (детали, сосуд с полосой наполнения, индикатор динамо; солнце
с подписью ушли из списка 2026-08-07 вместе с таймером, ряд слотов — 2026-08-08 вместе
с самим рядом, и это единственное удаление, которое ДВИГАЕТ подсказки: ряд стоял по
верху кадра, а ничьи `HintPlacement` разрывает вверх),
а `HintPlacement.Beside` берёт ближайшую к цели точку, где плашка не задевает ни одного
из них. Вторым критерием (в пределах +220 px по радиусу) идёт стрелка: она старается
не пересекать арт — но именно вторым, иначе «не перекрывает» уводит подсказку в дальний
угол и перестаёт быть «рядом». Держат это два теста: арифметика каталога
(`LevelCatalogTests.NoTeachingPlate_Covers…` + `…EveryTeachingPlate_StaysWithinReach…`) и
живой кадр (`GameFlowTests.TheTeachingPlates_CoverNoDetail…`) — первый доказывает, что
правило верное, второй что уровень действительно поставил подсказку по правилу.
*(До 2026-09-22 оба мерили нарисованную КНОПКУ; кнопок нет, гард переехал на
плашку — см. supersede выше.)*

**«ТАЩИ» едет с деталью.** Она ставилась один раз, в стартовую позицию детали, и там
и оставалась: деталь уезжала из-под собственной подписи, а плашка сидела поперёк кольца
прогресса — единственной обратной связи кручения. `HintPlacement.BesideTheThread`
считает её место каждый кадр: сбоку от нити, на расстоянии «радиус кольца + собственная
полудиагональ», так что никакой поворот плашки не достаёт до кольца, а пересечение с
нитью проверяется самим отрезком. Сторона запоминается между кадрами (иначе плашка
прыгает через деталь от сдвига в пиксель).

**Препятствие бита — ТРАЕКТОРИЯ детали, а не точка её старта** (`LevelScreen.AddDetailTrack`,
возврат дизайн-скептика 2026-09-22, блокер Б1). Подпись бита сбора ставится ОДИН раз и
не двигается (двигается «ТАЩИ» — это её работа), а искалась она против коробки 160×160 в
той позиции, где деталь стояла на старте бита. Дальше деталь ехала по нити и заезжала
ПОД плашку: на кадре `Game04_L1_tutorial_crank` плашка стояла x 511…1066 · y 227…376, а
самолёт с дома (881, 134) едет к ведёрку (549, 846) ровно сквозь этот коридор — из-под
подписи торчал один нос. Теперь в список препятствий кладётся цепочка прямоугольников
самого спрайта вдоль нити от текущего положения до сосуда — тем же приёмом, каким штрих
отгона подан цепочкой квадратов (`AddSwipeArrowCorridor`), и по той же причине:
bounding box диагонального пути — это почти весь кадр, а препятствие, не оставляющее
свободного места, отправляет поиск в откат «поверх цели». Держат это пиксельный гард
кадра (`GameScreenshotTests.AssertTheTravellingDetailIsVisible`: чернила детали на кадре
против кадра без подсказок, 98.4 % против 47.0 % на старой логике) и живой прогон всего
пути детали в `GameFlowTests.Tutorial_TeachesAim_ThenCrank_ThenSwipe_HoldingTheWaves`.

**Стрелка отгона — жест в палитре дропа, с НЕПОДВИЖНЫМ остриём.** Была кирпично-красная
(`#8f2f2c`) линия 1047 px от середины нарисованной мысли до (960, 1046): цвет из
грейбокс-эпохи, хвост перечёркивал арт, остриё упиралось в пустой асфальт. Цвет с тех пор
`#8fd7d6` — доминирующая краска самих кнопок дропа (замер по навoди / крути ручку / тащи:
45 756 px одного цвета), потому что у этого бита кнопки нет и стрелка — всё, что игрок от
него видит.

Второй заход (короткий штрих 240 px вниз от мысли) снял перечёркивание и принёс две новые
беды, обе поймал дизайн-гейт 2026-08-08: зазор 26 px отмерялся от ПРЯМОУГОЛЬНИКА мысли, а
пипсы с подложками рисуются НИЖЕ него, поэтому на кадре 05 стрелка росла прямо из пипсов; и
длина, и X приходили от мысли, поэтому единственная подсказка бита каждый раз указывала в
другое место неба. Поэтому у двух концов теперь разные роли:

| Конец | Кто им двигает | Почему |
|-------|----------------|--------|
| **остриё** | никто: `LevelScreen.SensorsCue` = (960, 1046) | оно НАЗЫВАЕТ вещь, которая есть в комнате — пульт с датчиками под экраном; имя обязано быть одним и тем же словом дважды |
| **хвост** | мысль + качание | начинается за `SwipeArrowGap` от всего, что мысль РИСУЕТ (`ArtThoughtView.DrawnBottomY` — блоб или низ диска под пипсами), и скользит по оси на 60 px |

Качание одностороннее: оно может стрелку укоротить, но не удлинить обратно В мысль, от
которой её только что отмерили.

**И слова к этой стрелке — «Маши над датчиком!» (решение founder 2026-08-08).** Она
села играть и не поняла, чего от неё хочет стрелка: стрелка, упирающаяся в предмет
мебели, называет МЕСТО, а механика — это движение. Кнопки «ТРЯСИ» в дропе нет (и глагол
был бы неверный с тех пор, как отгон уехал на датчики), поэтому бит получает
`HintPlate` — плашку дропа, нарисованную кодом, — через `ShowCardOnly`: плашка без
собственной стрелки, иначе бит говорил бы одно и то же двумя стрелками сразу.
*(Была `HintCard`, белая бумага грейбокс-стенда, — это и есть блокер Б1 дизайн-гейта
2026-08-08: один бит урока написан почерком прототипа, три соседних — почерком дропа.)*
Текст идёт через реестр `GameTexts.SwipeHint` — с 2026-09-22 это предложение
(«Это навязчивые мысли. Маши рукой над датчиком, чтобы отогнать их»), одной строкой —
ПК-скобка «(на компьютере — Q и A)» выведена в тот же день;
стенд в `Scene2ShakeAway` держит свою короткую подпись, у сценки нет места на фразу.
Место ищет `HintPlacement.Beside` по обычному
списку препятствий ПЛЮС сам штрих отгона — цепочкой квадратов 90 px вдоль него, а не его
габаритом: штрих идёт наискось через полкадра, и его габарит — полкадра, после чего
поиск не находил ничего и сваливался в «поверх цели», то есть ровно на чайку, которую
обходил. Карточка ставится один раз за бит и переставляется, только если мысль дрейфом
увела штрих ей под ноги. Кадры бита пересняты — `Game05_L1_tutorial_swipe` и
`Game29_L1_after_the_swipe_beat`; последний снимается теперь СЕКУНДОЙ ПОЗЖЕ (в прежнем
кадре догорал обломок лопнувшей мысли и горел красным индикатор динамо, и вместе это
читалось как срыв, а не как «дальше обычный play»).

**Титул говорит, КАК начать (решение founder 2026-08-08, переписано 2026-09-22).**
Запуск — два оборота динамо (`TitleScreen.StartDegrees` = 720°), и полоса оборотов
показывает их только ПОСЛЕ того, как ручку тронули. Founder не смогла стартовать.
Одна строка из `GameTexts` лежит на пустой полосе дороги под полосой: `TitleStart`,
40 pt. Второй, мельче, была ПК-скобка `TitleStartOnDesk` «(на компьютере — колесо
мыши)» — выведена 2026-09-22 вместе со всеми клавиатурными подсказками, «она играется
на автомате» (founder).

> **SUPERSEDED, плейтест founder 2026-09-22.** Здесь было «на готовом рендере есть
> нарисованная кнопка НАЧАТЬ» и `TitleStart` = «Раскрути ручку — два оборота».
> Ни того, ни другого больше нет:
> - **кнопки нет** — «кнопку НАЧАТЬ убрать» (нарисованная кнопка на стойке, где её
>   нечем нажать, заставила основательницу искать мышь). `TitleScreen` рисует на её
>   месте пустой `StartAnchor` (960, 864, 320×92) — **якорь под анимацию крутилки от
>   Кати**; тест держит его пустым, `Image` внутри = вернувшаяся кнопка;
> - **строка другая** — `TitleStart` = «Крути **крутилку**, чтобы начать» (её
>   формулировка, вернулась из `Withdrawn`; слово «ручка» заменено ею же
>   2026-09-22 — на пульте орган зовётся крутилкой, `system/CONTROLS_BRIEF.md`),
>   а «Раскрути ручку — два оборота» встречно **выведена**: количество показывает
>   полоса оборотов под словами, и говорить его дважды незачем.

Каждая строка рисуется дважды
(тёмная копия со сдвигом 3 px, потом светлая): на дороге лежат яблоки, цветок и кружка,
а подпись не имеет права читаться только между ними. Правило «поверх готового рендера
ничего не писать» этим не отменено — оно было про то, чтобы не перенабирать заголовок
дизайнера, а не про то, чтобы оставить игрока стоять у автомата; `GameTexts` из-за этого
снова стал реестром В ОБЕ стороны: `Withdrawn` — чего нельзя, `Live` — что можно, и
PlayMode-тесты обходят экраны по обоим спискам.

**S4 — два шага, а не один.** Бит награды сохранён (решение основательницы на
дропе): мысли растворяются 0.5 с → секунда тишины → сосуд выезжает в центр со всей
добычей 1.5 с → и только потом готовый экран `level-complete` 2 с. Это единственное
место во всём прогоне, где игрок видит собранное: ряд заполненных слотов и подпись
«Собрано: …» ушли вместе со старым макетом, а на финале сосудов больше нет.
Поверх этих 1.5 с с 2026-09-22 идёт наезд на ведёрко (`LevelView.SetVictoryZoom`),
а вместо фейда в чёрный дальше — переход «круглая рябь» (`RippleWipe`).

**S5 — готовый экран, который стирают.** `screens/game-over` ложится поверх кадра
непрозрачным, а каждый оборот динамо снимает `WipeSharePerTurn` (10 %) его альфы —
уровень проступает обратно ровно таким, каким его проиграли. Раньше экраном поражения
БЫЛИ мысли, и уровень приходилось сначала засыпать ими (`CoverScreen`), иначе
проигрышу по таймеру нечего было стирать (самого проигрыша по таймеру с
2026-08-07 тоже нет); теперь поле остаётся как есть.

### View — две композиции, один контракт

- `StageView` — greybox уровня 1 для стенда (дома, прямоугольники-детали).
- `LevelView` — уровень с артом: фон-плита, детали на авторских позициях,
  сосуд с полосой наполнения, чёрная маркерная штриховка мыслей (дроп 2026-08-05;
  до него — бирюзовые силуэты; светлая подложка под штрихами — [toggle], по
  умолчанию ВЫКЛ с 2026-08-08), HUD (индикатор динамо — солнце ушло с таймером
  2026-08-07, ряд слотов деталей выведен решением founder 2026-08-08), нить,
  неон-прицел, вуаль пика, кнопки обучения, готовый экран исхода.

Обе реализуют `ICollectionView` — это и есть причина, по которой цикл «двух рук»
не пришлось писать второй раз. `CollectionRuntime` получает свою композицию
(позиции деталей, центр сосуда, имена) снаружи, а не читает уровень 1 из статики.

Мысли уровня рисует `ArtThoughtView` (штриховка + пипсы, плюс подложка, когда её
включили тогглером), greybox — `ThoughtView` (блоб + подпись + пипсы); ряд пипсов у
них общий (`PipRow`, подложка под пипсами — опция, её включает только арт-путь, и она
НЕ ходит за тогглером мыслей: это читаемость счётчика ударов, а не обводка штриха).

### Mechanics (чистая логика)

| Класс | Что реализует |
|-------|----------------|
| `CrankSpeedMeter` | сглаживает рывковые дельты динамо в «скорость руки» (окно 0.25 с) |
| `CrankCollector` | MECHANICS §1: прогресс, порог, grace, сброс/таяние, влияние скорости |
| `SwipeDetector` | MECHANICS §3: удар = быстрый короткий сдвиг датчика высоты (`HeightA`/`HeightB`) — амплитуда 0.08 хода, резкость 0.6 ед/с, кулдаун 45 мс. Медленное движение, дрожь и **одно длинное движение** (=зажатая клавиша) ударов не дают: следующий удар ждёт, пока движение начнётся заново |
| `Thought` / `ThoughtField` | MECHANICS §3–4: волны, **общий запас мыслей на уровень (2026-09-22)**, дрейф с удержанием в кадре (`HeldInFrame`), прочность, затухание счётчика, таргетинг A/B/C, % перекрытия экрана |
| `GazeSelector` | SCREENS «выбор детали», вариант B: круг-взгляд, радиус [tune] (база 130 с 2026-08-07, было 90), удержание. Заморозки нет: джойстик — чистый прицел. С 2026-09-22 попадание — ПЕРЕСЕЧЕНИЕ круга с прямоугольником спрайта (`shapes`), а не расстояние до якоря: на самолёте У1 (484×84, центроид на 0.25) хвост был в 363 px от якоря при круге 130 и не ловился вовсе. Спор — по расстоянию до прямоугольника, ничья — по якорю; dwell не тронут |
| `LevelRules` | MECHANICS §5: победа (все детали), поражение (перекрытие), передышка. **Таймера нет с 2026-08-07** — см. ниже |
| `LevelOneData` | перенос уровня 1 из SCREENS.md: дома, детали, сосуд, HUD, палитра |
| `AudioMix` | MECHANICS §8: три слоя, кроссфейды, хвост медитации, громкость по числу мыслей (по КРИВОЙ с 2026-09-22), **кроссфейд при смене дорожки уровня**, мгновенная тишина |
| `DetailSweep` | SCREENS «Детали в сцене»: часы луча — когда идёт проход, где полоса, реже ли на поздних уровнях |

Ни один из них не трогает Unity-сцену — поэтому все правила проверяются
EditMode-тестами за миллисекунды, а сценки остаются тонкими.

**Отгон переехал с джойстика на датчики высоты** (решение founder 2026-08-07: «мы
действительно хотим отгонять на датчики движения»). Это не смена кнопки, а РАЗВЯЗКА:
джойстик носил два жеста сразу, и SCREENS приходилось разводить их порогом («плавный
наклон = взгляд, резкие смены = тряска») плюс заморозкой прицела на 0.15 с после каждого
удара. Ушло и то, и другое — прицел теперь работает каждый кадр, а `frozen` у
`GazeSelector` больше нет как параметра.

Датчик отдаёт не «нажали», а ПОЛОЖЕНИЕ (0..1), поэтому детект другой, чем у стика.
Удар = один проход руки: движение обязано быть и быстрым (`SwipeSharpness`, ед/с), и
длинным (`SwipeAmplitude`, доли хода) — накопленным ВНУТРИ одного направления, — а
следующий удар ждёт, пока рука повернёт обратно. Последнее правило и есть «одно движение —
один удар»: без него ровный подъём набивал бы удар каждые 0.2 хода, а на клавиатуре зажатая
Q играла бы отгон сама. Датчиков два, и они складываются: машешь над обоими — вдвое чаще.

**Числа порога — в единицах датчика, а не переименованные джойстиковые.** 0.5 отклонения
стика и 0.5 хода датчика — разные жесты, поэтому старую пару не перенесли: `SwipeAmplitude`
= **0.2** (пятая часть хода: рука проходит сильно больше, дрожь сигнала — сильно меньше),
`SwipeSharpness` = **0.6 ед/с** (половина скорости клавиатурной симуляции 1.25 и меньше
трети её пружины 2.0, так что обе половины взмаха проходят с запасом, а медленно
опускаемая рука — это отдых, а не взмах). Диапазоны слайдеров тоже сменились
(0.05–0.6 и 0.1–3) и прибиты тестом `TuningPanelCoverageTests.TheSwipeThresholds_…`.
Следствие персистенса: у полей новые ИМЕНА, поэтому старые ключи в `tuning.json`
игнорируются и числа стартуют с дефолтов — это и требовалось, старые значения означали
другое.

**Таймер убран целиком** (решение founder 2026-08-07: «давай уберём, он плохо
работает»). Это не упрощение правил, а снятие ВТОРОГО условия проигрыша: уровень
кончался двумя способами, и один из них был арифметикой, которой игрок не видел
— солнце в углу убывало, пока под ним шло то, про что игра. Ушли: `TimeLeft`,
`ClockPaused`, `LoseByTimer`, `LevelSeconds` (и глобальный, и пять пер-уровневых),
тогглер «таймер в обучении стоит», солнце-циферблат с подписью в HUD игры И на
грейбокс-стенде, столбец «Таймер» в лестнице §6. Осталось: победа = все детали,
поражение = перекрытие, передышка, авто-ретрай. Стенд тронут вместе с игрой
намеренно — стенд судит ПРАВИЛА, и стенд, у которого есть часы, которых нет в
игре, учит правилу, которого нет.

Что «нет таймера» — это утверждение о том, чего НЕ может случиться, поэтому у
него свои тесты (`LevelAndGazeTests.Level_IsNeverLostOnTime_TheClockIsGone`, три
минуты тиков на пустом экране; `LevelCatalogTests.TheLevelBands_CarryNoDuration`
— рефлексией по полям `TuningConfig`, чтобы ручка не вернулась «мёртвой»;
`GameFlowTests.TheOnlyDefeat_IsTheScreenFillingUp_AndTheHudCarriesNoClock` —
живой кадр без `Sun`/`SunDial`/`TimerLabel`/`TimerPlate`).

**Мысли разрастаются со временем жизни** (решение founder того же дня). Класс
S/M/L — это НАЧАЛЬНЫЙ размер (`Thought.SpawnSize`), а `Thought.Size` умножает его
на `GrowthScale` = clamp(1 + скорость %/с × возраст, 1, потолок), обе ручки
[tune per level]. Прежний инвариант «размер жёстко зажат классом» этим superseded.
Зажим снизу единицей — не перестраховка, а прямое слово founder: «мысли НИКОГДА
не спавнить меньше класса». Очевидная реализация «разрастания» — начать
маленькой и раздуться до класса — запрещена, потому что делает первые секунды
каждой волны слабее того состава, который стоит на панели; отрицательная ручка в
руками правленом `tuning.json` — тем же запретом. Обои экрана поражения
(`Wallpaper`) исключены: они подогнаны под ячейку сетки, и рост вытолкнул бы
картинку, которую ручка стирает, за кадр. Ореол `ThoughtBacking` масштабируется
сам — он берёт `thought.Size`, а радиус свечения считается от РИСУЕМОГО размера.

**Силуэт ПОКРЫВАЕТ класс, перекрытие считается по чернилам** (ревью Codex,
2026-08-08). `ArtLibrary.FitThought` масштабирует спрайт, пока обе стороны не
станут ≥ сторон коробки класса (cover, не contain), а `Thought.SpawnSize` берёт
`Max` от коробки и силуэта. Так «мысль НИКОГДА не меньше класса» стало
проверяемым на том, что реально спавнится: contain-фит нарушал это правило
этажом ниже теста — винная бутылка (холст 454×1521) в классе S спавнилась
42×140, и рост с перекрытием считались от огрызка.

Цена cover-фита — узкий силуэт торчит выше коробки (бутылка 180×603 в классе S),
поэтому перекрытие больше не считается по прямоугольнику. `Thought.Ink` —
доля прямоугольника, которую спрайт реально закрашивает
(`ArtLibrary.InkShareOf`, запечённые замеры по PNG: 15 % у вопросов У5, 64 % у
банкноты У2; мысли дропа — маркерная штриховка без заливки). `OverlapPercent`
перемножает слои: клетка под k мыслями закрыта на 1 − Π(1 − ink) — та же
арифметика, которой раньше уплотнялись силуэты в слотах HUD. Сплошной блоб (ink = 1: грейбокс-стенд и
обои `Wallpaper`) закрывает клетку целиком, поэтому всё, где арта нет, считается
ровно как раньше. Обои поражения освобождены от обеих правок: они и не мысли,
которые игрок отбивает, а утверждение S5 «экран целиком закрыт мыслями».
Запечённые доли перемеряются по самим PNG в
`LevelCatalogTests.EveryThoughtsInkShare_MatchesItsPng` — дроп не может оставить
их протухшими.

**Уровень, который нельзя проиграть, — это уровень без правил.** С тем же
ревью появился `LevelCatalogTests.EveryLevel_FillsUpAndIsLost_WhenNobodyBeatsAThoughtOff`:
модель уровня гоняется 3 минуты при НУЛЕВОМ отгоне, и каждый уровень обязан
дойти до порога поражения. До правки У1 и У2 не доходили никогда — лестница
прогрессии была монотонной из недостижимых ступеней.

### View

- `DesignStage` — канвас + **кадр 1920×1080**, вписанный (letterbox) в окно
  минус полоса панели тюнеров. Локальные координаты кадра = координаты
  SCREENS.md (origin слева-сверху, Y вниз). `DesignRectOf()` переводит
  отрисованный виджет обратно в дизайн-пиксели — на этом стоят layout-тесты.
- `Ui` — примитивы, говорящие в дизайн-пикселях: `Box`, `Rounded`, `Circle`,
  `Ring` (радиальный прогресс), `Dashed` (нить), `Label`, `Place`.
- `UiSprites` — процедурные спрайты (скруглённый прямоугольник в 9-slice под
  каждый радиус, круг, кольцо, пунктир). Арт — плейсхолдеры (WORLD.md).
- `StageView` — сама композиция уровня 1 и все её состояния (прогресс детали,
  нить, взгляд, мысли, индикатор динамо, сообщения). Солнце-циферблат и
  `StageOptions.TimerRunning` ушли отсюда 2026-08-07 вместе с таймером — стенд
  судит ПРАВИЛА, и стенд, у которого есть часы, которых нет в игре, учит
  правилу, которого нет. ~~Слоты; HUD одинаков во всех сценках: слоты есть
  всегда.~~ Ряд слотов выведен 2026-08-08 (founder: «убрать ряд совсем») и
  из стенда тоже — дрифт доки поправлен 2026-09-22.
  **Индикатор динамо стенд СОХРАНЯЕТ**, хотя из игры он выведен 2026-09-22
  («спидометр убрать из HUD игры»): сценка про динамо без динамо на экране
  судила бы правило, которого не показывает.
- `ThoughtView` — блоб мысли: подпись + ряд пипсов = оставшиеся удары.
- `HintPlate` — плашка дропа, нарисованная кодом: тёмная заливка RGB(35,52,65),
  белая строка, рамка 2 px, прямые углы. С 2026-09-22 несёт ВСЕ ЧЕТЫРЕ
  предложения обучения (с переносом строки); до того — только бит отгона, как фикс
  блокера Б1 дизайн-гейта 2026-08-08. Двустрочную форму (сноска мельче и приглушённее)
  плашка УМЕЕТ, но с 2026-09-22 ею никто не пользуется — в ней жила ПК-скобка.
  Заливка **непрозрачная** с возврата дизайн-скептика 2026-09-22: при α 0.93
  плашка отгона на светлом небе набережной читалась светлее трёх остальных —
  цвет одной и той же плашки решала картинка под ней (та же правка, что у полосы
  наполнения 2026-08-08).
- `RippleWipe` — переход «круглая рябь» (`Meditation/RippleWipe`, founder
  2026-09-22): круг воды из ведёрка, фронт с гребнями, четыре [tune]. Живёт на
  `GameFlow`, как и фейд, — переход переживает оба экрана, между которыми идёт.
  Без шейдера деградирует в плоский фейд, а не в мадженту.
- `HintCard` — обучающая карточка макета: белая, rx=18, рамка 4 px, кегль 44,
  цвет = контроллер (зелёный `#1a6e46` «крути», бирюзовый `#8fd7d6` «отгон», синий
  `#39587a` «оглядись»), стоит РЯДОМ с объектом и тянет к нему изогнутую
  стрелку. Тексты берутся дословно из реестра walkthrough (кадр 25). Бирюза
  отгона пришла с фикс-раунда 2026-08-07 — см. «Обучение».
- ~~`SlotSilhouette`~~ — **выведен 2026-08-08 вместе с рядом слотов** (решение
  founder «убрать ряд совсем»). Он делал силуэт детали читаемым в 72-px плашке:
  тинт `Image` УМНОЖАЕТ, поэтому на плашку попадала собственная альфа дропа, а она
  у него не форма (облака 0.64, лужа 0.55, лоза 111×1603 — полоска в четыре
  пикселя), и шесть из тридцати девяти деталей были пустыми плашками с метра.
  Спрайт рисовался девять раз одной краской по кольцу смещений: наложение делает
  жёсткой мягкую альфу (k копий = 1−(1−a)^k), кольцо ДИЛАТИРУЕТ тонкое. Из этой
  работы в игре остался `ArtDetail.IconCrop` — он теперь отвечает за добычу внутри
  сосуда, где вопрос тот же самый; `SilhouetteRadius` ушёл вместе с классом.
  Замер по всем 39 деталям в обоих состояниях: ink 9.8–99.9 %, контраст к плашке
  118–245 из 255 (критерий гейта — 8 % и 90).
- Индикатор динамо: зелёная дуга — это ЗОНА «хватает», она всегда зелёная;
  красным прибор загорается только когда простой реально стоит игроку детали.

**Добыча живёт в СИЛУЭТЕ сосуда, а не в его габарите** (`VesselHaul` +
`LevelDefinition.VesselSolid`). Прямоугольник сосуда — не сосуд: у кружки L2 в
него входит пар сверху и ручка справа, ведёрко L1 — трапеция в коробке. Сетка
добычи, центрированная по габариту, вывешивала стикер L2 наполовину мимо кружки.
Теперь у каждого сосуда в каталоге записана измеренная по альфе (> 0.9) коробка
«здесь точно сосуд», в долях его прямоугольника; `VesselHaul.InnerBox` центрирует
раскладку по ней и ужимает с запасом 6 %. Одно правило на три экрана: деталь,
падающая в сосуд, победный сосуд и ряд из пяти на финале.

**Победный сосуд нормирован к коробке макета, а не увеличен ×2**
(`LevelCatalog.VictoryScaleOf`). Кадр 18 рисует сосуд в прямоугольнике 480×300,
подпись «Собрано: …» на y 800 и ряд заполненных слотов под ней. Плоское ×2
давало ведёрку 480×778 — 72 % высоты кадра, и подпись со слотами оказывались
ПОД сосудом. Масштаб = min(×2, 480/ширина, 300/высота): пропорции сосуда свои,
коробка общая.

**Десатурация поражения — шейдер, а не тинт** (`Meditation/ThoughtFade`,
`Assets/_Project/Art/Resources/MeditationArt/shaders/`). Умножение не умеет гасить
цвет: старый тинт (0.72, 0.79, 0.79) на бирюзе дропа #5ADFE6 ПОВЫШАЛ насыщенность
кадра (0.474 → 0.492), потому что давил сильные каналы так же, как слабый. Гасить
— значит тянуть пиксель к его собственной яркости, и «лёгкая десатурация» на
ночной улице обязана ещё и высветлять; ни того, ни другого вершинный цвет не
может. Шейдер — стандартный UI плюс две строки (`_Desaturate` 0.68, `_Lighten`
0.12); те же числа продублированы в `ArtThoughtView.Drain()`, чтобы полосу можно
было проверить арифметикой без рендера. Замер кадра поражения (бирюза): 0.492 → 0.125
(полоса макета 0.07–0.26).

**Подложка под штриховкой — второй проход того же спрайта, и с 2026-08-08 она
ВЫКЛЮЧЕНА по умолчанию** (`Meditation/ThoughtBacking`, там же в `shaders/`).
Решение founder в живой сессии: «мысли должны быть чисто чёрными» — ореол читался
как белый блоб вокруг каждой каракули. Всё, что описано ниже, никуда не делось и
не стало неверным: это теперь [toggle] `TuningConfig.ThoughtBacking` на панели
игры, и founder включает его, чтобы сравнить на тёмных зонах плит — риск она
назвала сама. Тогглер читается КАЖДЫЙ кадр в `ArtThoughtView.Bind`, а не при
постройке: панель отвечает на «а так?» посреди уровня. Дроп 2026-08-05 сменил мысли
на чёрную маркерную штриховку #0D0D0D, и на замере кадров она села на плиты в
1.6–3.5:1 (медиана по уровню, 40–97 % чернил ниже 3:1; хуже всех набережная —
1.60). Тинт тут бессилен по той же причине, что и в десатурации: умножение не
делает чёрное светлым. Поэтому каждая мысль рисуется дважды — снизу тот же
спрайт плоским светлым `#F2F0EA` (этот цвет дизайнер поставила под свои полоски
образцов в `gameplay-walkthrough.html`), с альфой, **раздутой на 3 design-px**
(12 выборок: кольцо из восьми плюс диагональное кольцо в половину радиуса).
Получается ореол по каждому штриху, а не карточка за мыслью: SCREENS держит
мысли «без обведённой рамки», а экран под обоями поражения — «дырявым» (порог
считается по габаритам спрайтов ровно потому, что между штрихами видна сцена).
Радиус приходит в UV, а не в текселях: холсты дропа 454…1800 px рисуются в одни
и те же классы S/M/L, и фиксированный тексельный радиус дал бы 2 px на одной
мысли и 6 px на соседней; поэтому у каждой `ArtThoughtView` свой инстанс
материала (UGUI-`Image` не носит per-instance property block), который она
отпускает в `Dispose()`. 5 px пробовали и откатили: ореол начинает заливать
просветы штриховки, и «начирканное маркером» пропадает.

Пипсы поехали туда же. Было `#17595E` — бирюза дропа, затемнённая, чтобы читаться
НА силуэте; но у штриховки нет заливки, и пипсы оказались на голой плите при
1.3–1.9:1. Теперь это те же чернила `#0F0F12` на светлом диске подложки — и вот
эти диски за тогглером НЕ ходят. Решение 2026-08-08 названо про обводку штрихов;
ряд пипсов — счётчик оставшихся ударов, 12 px в поперечнике, и чёрная точка на
ночной улице перестаёт быть счётчиком. Обои поражения пипсов не носят вовсе, так
что «стена поражения без белого» выполняется буквально.

**Z-порядок — не константа, а тогглер.** База: сцена + полоса переднего плана →
детали → нить → мысли → сосуд → HUD → подсказки → готовый экран исхода
(`OutcomeLayer` выше даже HUD: отрисованный экран закрывает кадр целиком). Это список SCREENS.md с одним
уточнением: полоса переднего плана отделена от сосуда, потому что в walkthrough
(кадры 4–6) едущая деталь и нить видны поверх полосы.

Последние два слоя меняются местами по `[toggle]` «мысли закрывают сосуд и
деталь» (MECHANICS §4, `StageView.ApplyCoverToggle`):

| Тогглер | Порядок | Что видно |
|---------|---------|-----------|
| выкл (дефолт) | … → мысли → **сосуд** → HUD | сосуд и перетаскиваемая деталь всегда читаются поверх мыслей |
| вкл | … → **сосуд** → мысли → HUD | мысли накрывают сосуд и текущую деталь (пик хаоса, кадр 16) |

Перетаскиваемая деталь с кольцом прогресса на время сбора переезжает в слой
сосуда — поэтому один тогглер управляет обоими, ровно как сформулировано в §4
(«могут ли закрывать сосуд и деталь, которую сейчас тянешь»). HUD выше в обоих
случаях: по SCREENS.md мысли его не закрывают.

Порядок выкладывается **целиком** (`ApplyLayerOrder`), а не подвижкой одного
слоя мимо другого: подвижка забывала, что вынутый из списка слой сдвигает все
верхние на единицу, и после «включил–выключил» сосуд оказывался выше вуали пика.
Обратимость держит PlayMode-тест на трёх кругах тогглера.

**Экран поражения — исключение из тогглера.** `LevelView.StageDefeat()` ставит
мысли поверх сосуда независимо от `[toggle]` и оставляет сосуду его собственный
цвет (полупрозрачность тогглера — для сосуда, в который ещё целятся). Иначе
«экран целиком закрыт мыслями» (SCREENS S5) не выполняется картинкой: обои
закрывали кадр на 100 %, а портфель и деталь на нити продолжали светиться в
середине. `ClearDefeatStaging()` возвращает порядок тогглера на рестарте.

### Арт-дроп: откуда взялись позиции

`Assets/_Project/Art/Resources/MeditationArt/` — пять плит 1920×1080, объекты и
мысли, плюс с дропа 2026-08-07 готовые экраны, кнопки и семь треков. Спрайты
**обрезаны по альфе при импорте** (порог alpha > 4 — тот же, которым импортирован
первый дроп), поэтому размер в каталоге — это сама нарисованная форма, без невидимых
полей, которые сдвигали бы деталь мимо её места. Рендеры экранов пришли в 3840×2160 и
даунскейлены ×0.5 до кадра 1920×1080 (SCREENS «S1–S6»). Настройки импорта ставит
`ArtImportSettings` (AssetPostprocessor), а не рука: переимпорт и свежий клон дают то
же самое.

**Перенумерация 2026-08-07 не тронула ни одного числа.** Новый порядок — набережная
(5 деталей) → офис (5) → метро (6) → библиотека (8) → город (8); вперёд вынесены
сцены, где меньше объектов. Спрайты объектов и мыслей дроп прислал байт-в-байт теми
же (сверено хешами), а плиты — теми же композициями, умноженными на ×0.49 (замерено
по каждому уровню: средний коэффициент 0.456…0.497 при неизменной композиции). Поэтому
содержимое сцены переехало вместе с её номером — позиции, габариты, кропы иконок,
силуэт сосуда и пер-уровневые сдвиги HUD. Ничего не перемерялось, и это главное:
перемер был бы поводом потерять решение.

Из состава ушли **две лужи** (метро и город) — в отличие от впечённого облака это не
декор, спрайтов больше нет вообще. С ними отменился один сдвиг HUD: индикатор динамо
в метро уезжал вправо по полу (140 → 420) ради лужи, и вернулся на базу SCREENS.
`LevelCatalogTests.TheMetrosCrankIndicator_CameBackToTheScreensBase_WithThePuddleGone`
держит это отдельным тестом, потому что «HUD подвинут» — это долг, а не фича: без
теста, который говорит «у этого была причина и причина ушла», сдвиг переживает свой
повод навсегда.

`Resources` — потому что экраны собираются процедурно и ссылок в сцене нет;
префикс `MeditationArt/` — требование ARCADE_INTEGRATION_CONTRACT §2 (в общем
проекте автомата семь игр, коллизия имён иначе неизбежна).

**Позиции и размеры в `LevelCatalog` сняты с `превью.png`, а не с таблиц.**
Инкремент делает превью авторитетом («при расхождении авторитет — превью»), и
буквально: каждый спрайт найден внутри превью маскированным template-matching'ом
по масштабу, после чего весь каталог собран обратно в картинку и сдиффен с
превью (среднее |Δ| ≈ 6–12 из 255 на первом дропе, 5.1 и 2.7 на уровнях 4–5;
всё, что осталось, — мягкое свечение краёв самого дропа: у голой плиты
без объектов |Δ| выше — 8.4 и 4.4). Где таблица SCREENS и превью разошлись,
выиграло превью: портфель L5 на y 904, а не 935; кружка L2 на (1094, 891) и
292×280, а не (1060, 935) и 250×230 — её
прямоугольник включает пар над чашкой.

Два наблюдения второго дропа стоят того, чтобы быть записанными:
**детали уровня 5 нельзя было выбрать по счёту совпадения.** В офисной плите
запечены два жёлтых стикера-обманки, и один из них совпадает со спрайтом
`sticky-note` ровно так же хорошо, как настоящая деталь (разница верхних
пиков — 0.1). Отличает их не матчинг, а то, что обманки ЕСТЬ в самом
`background.png`, а деталь — нет. И **самолёт уровня 4 нарисован со следом**:
его прямоугольник 484×84, а центр приходится на середину следа, не на
фюзеляж — как лоза уровня 1. Сам спрайт при этом отличается от самолёта на
превью формой крыльев (вопрос дизайнеру, SCREENS «Оставшиеся вопросы»).

**Прямоугольник детали — не сама деталь: `ArtDetail.AlphaCentroid`** (фикс-раунд
дизайн-гейта 2026-08-07). Наблюдение про самолёт со следом было записано, но из него
ничего не следовало, и после перенумерации он стал деталью УРОВНЯ 1 — того самого,
на котором идёт обучение. Кольцо прогресса обводило пустое небо, нить стартовала
в 145 px от фюзеляжа. Поэтому у каждой детали в каталоге лежит взвешенный по альфе
центр её собственного PNG, в долях габарита: у самолёта 0.2497 по X — это design
(760, 135), корпус; у компактных спрайтов он и есть центр (луна и раскрытая книга
измерились в 0.5000). К нему привязано всё, что ПРИЦЕПЛЯЕТСЯ к детали — кольцо
(`LevelView`), нить (`CollectionRuntime.SetThreadOffsets`), стрелки подсказок и место
обучающей мысли; позиционирование самого спрайта осталось на `Home`, потому что там
его рисует дроп. Инвариант держит `LevelCatalogTests`: центроид обязан лежать ВНУТРИ
прямоугольника детали, и у всех, кроме самолёта, — не дальше 20 % габарита от его
центра (то есть это измерение, а не лазейка переставить композицию).

**Третье наблюдение, уже из дизайн-гейта: дроп кое-где отдаёт объект ДВАЖДЫ** —
запечённым в плиту и отдельным спрайтом. Такая «деталь» ломает не тест, а смысл:
собрал — а копия висит на месте. Найдено две. Облако «у провода» L5 впечено в
`background.png` и прислано файлом `cloud-2.png`: решение founder (2026-08-01) —
пока фон не правлен, это декор, ровно как жёлтые стикеры офиса, и город стал
**восемью** деталями. Лампа L4 стояла в каталоге на столике (867, 792), где в
плите живёт впечённая лампа; превью рисует свободную лампу на правой скамье, и
это оно и есть — композит спрайта на плиту роняет ошибку с 24.2 до 7.2 из 255 в
точке (1330, 817), 57×99. Обе истории держит `LevelCatalogTests`, чтобы «в дропе
файлов больше, чем в каталоге» читалось как решение, а не как пропажа.

**Дроп 2026-09-25 (Катя): восемь ассетов, из них три — прятки.** Заведены под
прежними ключами там, где это ЗАМЕНА рисунка (`L2/objects/flower` ← «горшок с
ножками», `L5/objects/curtains` ← «Window 1», `L5/objects/moon` ← новая луна), и
новыми ключами там, где это второй рисунок к существующей детали
(`L1/objects/shark-whole`, `L3/objects/goose-whole`, `L5/objects/window-lady`).
Плюс перерисованные `screens/title` и `screens/finale`. Две кнопки дропа не
вшиты — решение founder (тексты на них неправильные), долг Кате.

**Механика пряток живёт в ДАННЫХ детали, а не в трёх ветках кода**
(`ArtDetail.CaptureSprite/Size/Offset` и `ArtDetail.BehindSprite/Size/Offset`,
MECHANICS §9). Фразы founder — «видим всю акулу только, когда начинаем её
перетаскивать», «тело за пределами экрана», «когда шторки перетаскиваем, под
ними окно с дамой» — это два хода, а не три: фрагмент, который ЕСТЬ целое
(подмена спрайта и прямоугольника у той же детали), и фрагмент, который
ЗАКРЫВАЕТ другое (второй `Image` на слое сцены, включаемый под деталью).
`LevelView` держит по слоту на деталь в обоих списках и переключает их одной
функцией `SetCaptured`; каталог отдаёт `DrawnSpriteOf` / `DrawnSizeOf`, и всё,
что считает про нарисованную картинку, — луч, неон-ободок, иконка добычи —
спрашивает их, а не `Sprite`/`Size` напрямую.

**«Где сейчас деталь» — один ответ на всю игру: `LevelCatalog.DrawnRectAt(spec,
position, captured)`** (плюс `DrawnRectSpanAt` — покой ∪ захват, для тех, кому
ответ нужен на ОТРЕЗОК времени, а не на кадр). У прятки правдоподобных
прямоугольников три — каталожный, фрагмента и того, чем деталь оказалась (акула
стоит на полкорпуса в стороне от плавника), — и каждый потребитель, выбравший
свой, даёт один и тот же класс бага: игра рисует акулу, логика рассуждает про
плавник. Ревизия Codex 2026-09-25 нашла ровно двух таких потребителей, и оба
теперь зовут хелпер:

- `LevelScreen.AddDetailTrack` (препятствие сбора-бита) — `DrawnRectSpanAt`:
  плашка ставится ОДИН раз на весь бит, а прятка проявляется посреди него, и бит
  не знает, в какой точке нити это случится (это прогресс коллектора, т.е. как
  сильно крутят), поэтому каждое звено цепочки держит обе картинки.
- `LevelView.SetDetailProgress` (кольцо «скрыта мыслью» + место самого `Image`) —
  `DrawnRectAt` по текущему состоянию захвата: средняя мысль 280×220 глотает
  плавник 196×63 целиком и не может проглотить акулу 292×180, так что старая
  арифметика гасила кольцо над отлично видимой акулой.

Гарды — `LevelCatalogTests.TheDrawnRectangle_…` (хелпер как таковой),
`LevelCatalogTests.TheSborBeatsObstacle_IsTheWholeAnimal_NotTheFragmentItRestsAs`
(блокер 1) и `GamePictureTests.TheRingOfARevealedHideaway_IsJudgedAgainstTheAnimal_NotTheFragment`
(блокер 2). Оба пробника переписывают арифметику прямоугольника ОТ РУКИ, а не
зовут `DrawnRectAt`: пробник, который делит шов с проверяемым кодом, уедет вместе
с ним обратно в баг и останется зелёным.

Порог захвата — `LevelView.IsCaptured`: замечена, не проскальзывает, прогресс
выше порога дуги кольца. Намеренно НЕ `CrankCollector.IsSpinning`: этот флаг
падает на каждом кадре перехвата руки на динамо, и привязанная к нему акула
мигала бы обратно в плавник дважды в секунду на глазах у игрока.

Регистрация двух картинок друг с другом — измеренная. Плавник и акула сходятся
на ватерлинии (рябь плавника — y 110 его 130-px холста; эллипс воды акулы —
y 420 её 719 px рисунка); `L3/objects/goose` буквально совпадает с правым
фрагментом целого гуся (template-matching: масштаб 1.00, смещение (432, 318),
среднее |Δα| 0.015); штора и дама нарисованы одной композицией на одном 600-px
холсте, поэтому кладутся по одному отображению ×0.25.

**`ArtDetail.EdgeFadeUv` снят вместе со свойством шейдера `_EdgeFadeUV`.** Это
был точечный костыль под ОДИН холст: старая `moon.png` обрывала своё сияние на
α = 5/255, а UI композитит в линейном пространстве, поэтому над ночным небом это
давало жёсткий шаг в 12 единиц sRGB — «квадрат вокруг луны» плейтеста
2026-09-22. Новый ассет — тот же 220-px диск на 380-px холсте (было 352), сияние
успевает погаснуть внутри, а оставшиеся на границе α = 2 сняты при заводе в
Resources. Гард не исчез, а стал измерять ассет вместо признака:
`LevelCatalogTests.NoSquareAroundTheMoon_AndNoSpriteNeedsAnEdgeFadeAnyMore`
читает PNG, берёт худший пиксель границы, композитит его на самое тёмное небо
под луной в линейном пространстве и требует шага ≤ 1 из 255 — и отдельно
требует, чтобы поле `EdgeFadeUv` не вернулось в `ArtDetail` (на остальных 31
детали, обрезанных по альфе, такое гашение съело бы их собственные чернила).

Сосуд уровня 3 запечён в плиту. Отдельного спрайта нет — `ArtLibrary.VesselOf`
вырезает сумку прямо из текстуры фона (`Sprite.Create` только ссылается на
область, копии нет), и этот же спрайт носят карточка уровня, победный сосуд ×2
и финал. Наполнение в самом уровне — оверлей-индикатор поверх запечённой сумки.

### Звук — три слоя (MECHANICS §8)

Правила живут в `AudioMix` (чистая арифметика), три `AudioSource` — в `GameAudio`,
а владеет ими **`GameFlow`, а не экран**: карточка живёт 2.5 с, и фон, который
пересобирают на каждой из них, — это пять провалов в музыке за прогон. Экран только
ОПИСЫВАЕТ, как он звучит (`GameScreen.Audio` → `AudioScene`), а микс решает, что это
значит.

Разделение не косметическое. Всё, что §8 утверждает, — это утверждение о ЧИСЛЕ во
ВРЕМЕНИ («кроссфейд 0.4 с», «доигрывает ещё 1.5 с», «громкость растёт с числом
мыслей»), а в batchmode звукового устройства нет: тест, проверяющий «играет ли
медитация», проверял бы тишину в обоих случаях. Поэтому все правила — в EditMode
(`AudioAndSweepTests`), а PlayMode держит только проводку: у каждого уровня свой
трек, и «в меню» действительно ОСТАНАВЛИВАЕТ источники, а не просто выводит их в
ноль.

Импорт — `ArtImportSettings.OnPreprocessAudio`: **Streaming, не decompress-on-load**.
Библиотека — 18 МБ mp3, метро — 11; распакованные в память это сотни мегабайт PCM в
процессе автомата, где живут семь игр, и Unity декодировала бы их все до первой
карточки уровня.

Один толкованный пункт, а не выведенный: «на экранах S1/S2/S4/S5/S6 фон уровня не
играет [toggle], старт — тишина, **кроме титула**» прочитано как «титул — названное
спекой исключение, он играет трек первого уровня». Тогглер `AudioSilentOffLevel`
управляет остальными четырьмя.

### Луч-подсветка деталей (SCREENS «Детали в сцене»)

«Светит по спрайтам деталей, а не по фону» — это и есть причина, по которой луч
шейдер (`Meditation/DetailSweep`), а не белый прямоугольник, едущий по экрану:
прямоугольник светит по плите, а плита обязана остаться тёмной (дроп 2026-08-07
затемнил все пять ровно затем, чтобы объекты читались). Маска — СОБСТВЕННАЯ альфа
спрайта, и накладывает её сам блендинг (`Blend SrcAlpha OneMinusSrcAlpha`): шейдер
прибавляет свет плоско, а на экран он приходит уже умноженным на альфу. Маска
бесплатная, любой формы и **ровно одна**.

Ровно одна — потому что две стоили фиче гейта. До 2026-08-07 полоса ещё и явно
умножалась на `color.a` внутри шейдера, то есть на экран приходило
`сила × полоса × α²`. На плотном арте разницы нет (α = 1), но половина деталей игры
тонкие или полупрозрачные — очки библиотекаря нарисованы сглаженной проволокой и
полупрозрачными линзами, облако у столба и ореол луны это полупрозрачные пятна, —
и они получали ×0.13 света вместо ×0.36. На штатной силе 0.45 это +11…15 единиц из
255 против +25…+124 на плотных деталях: то самое «луча не видно», которое принёс
дизайн-скептик. Лишний множитель убран, сила слайдера и её диапазон не тронуты.

Позиция полосы приходит в UV, а не в пикселях, по той же причине, что радиус у
`ThoughtBacking`: детали идут от 40-пиксельной скрепки до 484-пиксельного следа
самолёта, и один тексельный размер был бы половиной первой и двадцатой долей
второго. `DetailSweep` считает полосу в design-px, `LevelView.ApplySweep` переводит
её в UV каждой детали, у каждой — свой инстанс материала (UGUI-`Image` не носит
property block), который уничтожается вместе с уровнем.

Луч гаснет на пике хаоса: полоса света и затемнённые края — две картинки, спорящие
об одном кадре.

**Чем меряется, что луч виден.** Тремя разными вопросами, потому что первые два
были зелёными ровно тогда, когда человек не видел ничего:

- `GameFlowTests.TheLightSweep_ReachesEveryDetailsOwnMaterial_AndGoesOutAtThePeak` —
  число доехало до материала каждой детали. Это проводка, а не картинка.
- `GameScreenshotTests.TheLightSweep_VisiblyBrightensEveryDetailItIsOver` — полоса
  ставится на альфа-центроид КАЖДОЙ детали ВСЕХ пяти уровней, кадр с полосой и кадр
  без неё снимаются тем же `StandTestHarness.Capture`, что пишет кадры гейта, и
  прибавка меряется как p95 подъёма по сдвинувшимся пикселям (не percentile яркости
  прямоугольника: подсветка чёрной оправы очков — это +100 на тёмном конце
  гистограммы, а p95 прямоугольника там — бледная линза). Плюс минимум сдвинутой
  площади (2 %), проверка «при силе 0 не сдвинулось ничего» и старая проверка, что
  голая плита рядом не шелохнулась.
- `TheOffice_/TheCity_LightSweepAcrossItsDetails` — сами кадры 28 и 26 меряют себя:
  после съёмки полоса гасится, и деталь под ней обязана потерять ≥20 единиц p95.
  Кадры до этого стажировались по числам самого луча («идёт», «сила ≥ 0.9×0.45»,
  «центр внутри прямоугольника детали»), все три числа были верны, а картинку гейт
  прочёл как «луча нет» — числа никогда не были утверждением, пиксели были.

### Неон-прицел и неон-обводка деталей (заказ founder 2026-08-07)

Обе фичи — свет, ДОБАВЛЕННЫЙ шейдером (`Blend SrcAlpha One`), а не краска,
положенная поверх, и обе по той же причине, по которой луч деталей шейдер:
умножающий UGUI-тинт на тёмной плите не умеет быть ярче плиты, а на светлой
превращается в серое пятно.

**Прицел — один квад вместо трёх примитивов** (`Meditation/GazeNeon`). Было:
полупрозрачный диск @0.25, спрайт пунктирного кольца и дуга удержания. У всех
трёх были зелёные layout-тесты — прямоугольник на месте, нужного размера — и
невидимая на фотографической плите картинка; основательница просто не находила
прицел. Шейдер знает расстояние пикселя от центра квада, чего текстура кольца
знать не может (её пришлось бы перерисовывать под каждый радиус, а радиус теперь
слайдер), и раскладывает его в три полосы: тугая гауссиана на кольце (трубка),
вчетверо более широкая и вчетверо более слабая вокруг (стекло — именно она
читается как неон) и слабая заливка внутри, чтобы у прицела было ТЕЛО.
`GazeSelector.Radius` стал свойством над `TuningConfig.GazeRadiusPx` (база 130
вместо 90): один радиус и для рисунка, и для хит-теста — два числа сделали бы
«крупнее» ложью, которую не видно, потому что круг обводил бы не то, что
выбирает. Квад рисуется в 1.45 раза шире круга (`GazeQuadMargin`), иначе свечение
обрезается и остаётся ровно тот жёсткий контур, который был невидим.

Первый прогон гейта эта фича провалила на строчке, которой в шейдере нет:
`Ui.NewImage` отдаёт RectTransform с якорями в ЦЕНТРЕ родителя, а все координаты
проекта — от левого верхнего угла, так что `Ui.MoveTo(…, 960, 420)` уносил круг
на полтора экрана вбок. Пиксельный тест увидел «прицела нет»; `Ui.Place` в
`BuildGaze` — вся починка.

**Обводка деталей — ребёнок спрайта** (`Meditation/DetailOutline`, [toggle], по
умолчанию ВЫКЛ). Маска — собственная альфа спрайта, расширенная на N дизайн-px
теми же двенадцатью выборками, что у `ThoughtBacking`, МИНУС сама альфа: остаётся
ободок снаружи чернил. Вычитание — это и есть причина, по которой обводку можно
рисовать ПОВЕРХ спрайта, а рисовать поверх — причина сделать её `Image`-ребёнком
детали: она даром наследует и позицию на нити, и масштаб пульса, и переезд в слой
сосуда на время перетаскивания, и исчезновение после сбора. Соседом её пришлось
бы вести через все четыре руками — ровно то, чем занят `SetDetailProgress` ради
одного кольца. Выключена она НЕ прозрачностью, а `SetActive(false)`: сравнение
трёх подсказок портит и обводка альфой ноль, если она всё-таки что-то двигает.

**Радиус ободка — не ручка с панели, а ручка, зажатая толщиной самого рисунка**
(`LevelView.RimRadiusPx`, фикс-раунд дизайн-гейта 2026-08-08). Дилатация остаётся
обводкой, только пока она уже того, вокруг чего идёт: ободок радиуса r прибавляет к
каждому штриху 2r, поэтому на отгружаемых 6 px офисная скрепка — 10.7-пиксельная
проволока в своём месте на плите 40×56 — выходила сплошным бирюзовым пятном, а очки
библиотекаря теряли обе линзы и перемычку. Правило: **ободок вправе прибавить штриху
не больше половины его собственной ширины**, то есть r ≤ ¼ толщины. Толщина —
`ArtLibrary.StrokeThicknessOf`, запечённая по каждому PNG как 2 × площадь чернил /
периметр чернил (для полосы шириной w это ровно w, для круглого пятна — радиус, то
есть «ограничения нет») и приведённая к размеру, в котором деталь РИСУЕТСЯ: холст
скрепки 79×113, её место на плите 40×56, и предел, взятый в пикселях холста, был бы
вдвое щедрее картинки. Незамеренный спрайт получает число с панели — таблица может
ободок только сузить, не расширить. Держат это два теста:
`LevelCatalogTests.EveryDetailsStrokeThickness_MatchesItsPng` (дроп не может оставить
числа протухшими) и `…NoDetailsNeonRim_IsWiderThanItsOwnStrokeCanCarry` — арифметика по
ВСЕМ деталям всех пяти уровней, потому что кадр показывает только одну.

**И квад ободка шире спрайта.** Ободок по определению снаружи чернил, а квад,
кончающийся на кромке спрайта, класть его некуда: у тапок офиса он был срезан прямыми
по краю (тот же гейт). Поэтому прямоугольник растягивается на радиус + 2 px в каждую
сторону (`_PadUV`), а шейдер отображает выборки обратно внутрь рисунка; выборка,
попавшая ЗА спрайт, отвечает нулём, а не зажатым краевым текселем — иначе спрайт,
касающийся своей кромки, размазал бы крайний ряд в бирюзовую юбку. Где именно лежит
спрайт в текстуре, шейдер получает как `_UvRect`: «снаружи» — это утверждение о
СПРАЙТЕ, а не о текстуре.

**Полоса наполнения — под сосудом на всех пяти уровнях** (`LevelCatalog.
VesselBarRectOf`). Была уловкой уровня 3, где сосуд запечён в плиту и «наполнение»
некуда положить; оказалось, что остальным четырём её не хватало ровно так же —
добыча внутри ведёрка читается, только если уже знаешь, сколько деталей на
уровне, а ряд слотов лежит в другом конце кадра. Метро не тронуто ни на пиксель,
и то, что это возможно, — доказательство, что специфичного для метро в этом
виджете не было ничего, кроме места. Прямоугольник считается в каталоге, а не во
вьюхе, с одним прижимом: рюкзак библиотеки — 230 px по высоте с центром на
y 974, то есть его собственный низ на девять пикселей ЗА кадром, и «под сосудом»,
понятое буквально, увело бы полосу за экран на том самом уровне, где деталей
больше всего. Держит это `LevelCatalogTests.TheVesselBar_IsOnScreenAndUnderIts
Vessel_OnEveryLevel` плюс живой кадр на все пять уровней.

**Полоса живёт на слое HUD, а не на слое сосуда** (дизайн-гейт 2026-08-08). SCREENS
относит её к HUD — «мысли HUD не закрывают», — и она была единственным виджетом HUD,
который это правило нарушал, просто потому что собирался в `BuildVessel`, рядом с тем,
подо что он встаёт. При включённом тогглере «мысли закрывают сосуд» (пик хаоса, полоса
переднего плана офиса) слой мыслей рисуется ВЫШЕ сосуда, и полоса пропадала под блобами
ровно в тот момент, когда она — единственное, что говорит, сколько уровня осталось.
Разделение получилось честное: ГДЕ она стоит — по-прежнему дело сосуда
(`LevelCatalog.VesselBarRectOf`), а что её вправе закрыть — дело HUD. Побочно она
перестала гаснуть вместе с `CanvasGroup` сосуда, и это то же самое утверждение.

### Tuning

- `TuningConfig` — **статические** поля: `[tune]`/`[toggle]` из MECHANICS.md §1–§5.
  Статика намеренная: значения обязаны переживать переключение сценок (done
  contract §7). Это состояние стенда, игровой код на него опираться не должен;
  финальные числа после плейтеста переезжают в MECHANICS.md.
  Стартовые значения лежат в `TuningConfig.Defaults` (константы) — тесты
  сверяются с ними, а не с литералами, чтобы перетюнивание баланса не красило
  suite в красный.
- `TuningStore` — **персистенс значений** (см. §3 «Где живут значения панели»).
- **[tune per level]** (MECHANICS §6): полосы значений уровней лежат плоскими
  статиками `L1…/L2…/L3…/L4…/L5…` — не массивом. `TuningStore` персистит рефлексией
  каждое изменяемое статическое поле, и его круговой тест ходит по тому же
  набору, так что пер-уровневая ручка сохраняется, восстанавливается и покрыта
  тестом в момент объявления; массив потребовал бы второго сериализатора и
  второго теста, чтобы ему верить. `ApplyLevel(i)` копирует полосу в живые
  значения при входе в уровень — правила продолжают читать простые статики.
  Следствие: старт уровня перезаписывает значения, общие со стендом. Это
  сознательно — после этого инкремента тюнинг живёт в уровнях, а стенд не
  зависел от конкретных чисел (его тесты читают `Defaults`).
- «Порог пика хаоса» — **отдельный** `[tune]` от порога поражения. В SCREENS это
  два разных параметра, и иначе быть не может: пик, начинающийся там, где уровень
  кончается, никто никогда не увидит. Стенд их когда-то склеил.
- `TuningCatalog` — какие строки показывает каждая сценка (§7 MECHANICS) и что
  показывает панель игры: секция «У{n} · …» на каждый уровень плюс общие
  значения. Все пять секций на панели сразу, а не за вкладкой активного уровня —
  основательница крутит давление пятого уровня, пока под руками первый, а
  панель, которая перетасовывается на каждой карточке, — это панель, в которой
  пять раз за прогон заново ищешь своё место.
  **Состав панелей зафиксирован тестом** `TuningPanelCoverageTests`: он сверяет
  НАБОР ключей каждой сценки с перечнем по MECHANICS/SCREENS (не количество —
  счётчик когда-то пропустил целую группу §4) и проверяет диапазоны слайдеров
  против спека (grace 0–800, сбор 3–15, прочности 2–12, затухание 400–1500,
  дрейф 20–60, перекрытие 85–100, уровень 60–180, передышка 0–5).
- `TuningParam` / `TuningPanel` — декларация «лейбл + диапазон + геттер/сеттер»
  и её отрисовка слайдерами/переключателями. Панель правит `TuningConfig`
  мгновенно — пересборки не нужно; PlayMode-тест двигает реальный экранный
  слайдер и проверяет, что механика меняется в том же прогоне.
  **Вёрстка панели — четыре полосы, которые делят её высоту без остатка**
  (`TuningPanel.ResolveZones`, чистая арифметика → EditMode-тест): шапка →
  окно строк (`ScrollRect` + `RectMask2D`, скроллится, если список выше окна) →
  блок живых показаний → нижняя полоса кнопки «скрыть панель». Раньше строки
  верстались от верха панели, а показания висели на её нижнем крае — на любом
  окне ниже 1080 они наезжали друг на друга во ВСЕХ сценках; теперь пересечение
  невозможно по построению, и это проверяется PlayMode-тестом по видимым
  (обрезанным маской) прямоугольникам на маленьком вьюпорте. Кнопка «скрыть
  панель» возвращает кадру всё окно (в этом состоянии снимаются скриншоты для
  дизайн-гейта); поля вокруг кадра — в палитре стенда, не чёрные, чтобы цвет
  композиции можно было судить прямо в рабочем виде.

### Stand

- `PreviewSceneController` — общий каркас сценки: собрать `DesignStage`,
  `StageView`, панель; каждый кадр прочитать `ArcadeInput`, посчитать скорость
  динамо и удары отгона (взмахи над датчиками), отдать управление правилам сценки.
- `CollectionRuntime` — общий цикл «двух рук» для сценок 3 и 4: выбор детали
  (A/B/C), перетаскивание кручением, поле мыслей, синхронизация вида.
- `PreviewStandNav` — навигация + единственная точка выхода.
- `MenuButtonExit` — подписка на `ArcadeInput.MenuButton.Pressed`.

## 3. Контракты

### Ввод — только `ArcadeInput`

Пакет `com.aigamestudio.arcade-controls` подключён по file-пути в
`Packages/manifest.json`. Игровой код читает **исключительно**
`ArcadeInput.Crank / Joystick / HeightA / HeightB / GreenButton / MenuButton`;
клавиатурная симуляция живёт внутри пакета (`ArcadeInputRunner` + `KeyboardBackend`).
EditMode-тест `ArcadeContractTests` сканирует `Scripts/` и падает, если в
игровом коде появится `UnityEngine.Input`, `InputSystem`, `Keyboard.current`
и т.п. Единственное место, где редактор ссылается на Input System — сборщик
сцен (Editor-only), которому нужен `InputSystemUIInputModule` для мыши в панели
тюнеров.

**Три контроллера, по одной строке на каждый** (решение founder 2026-08-07):

| Контроллер | Что делает | Кто читает |
|---|---|---|
| Ручка-динамо | тянет замеченную деталь | `CrankSpeedMeter` → `CrankCollector` |
| Джойстик | **только прицел** (круг взгляда) | `GazeSelector` |
| Датчики высоты A/B | **только отгон** (взмахи) | `SwipeDetector` → `ThoughtField` |

Сканер контракта этого не видит — «не читать клавиатуру» одинаково верно и для кода,
который не читает НИЧЕГО, — поэтому у датчиков свой тест
(`ArcadeContractTests.TheOtgon_IsReadFromTheHeightSensors_ThroughArcadeInput`): оба места,
где качается цикл (`GameFlow`, `PreviewSceneController`), обязаны брать обе высоты именно
из `ArcadeInput`. А то, что стик больше НЕ бьёт, держит живой кадр во всех трёх режимах
выбора детали (`GameFlowTests.TheJoystick_LandsNoHits_InAnyNoticeMode_ButTheSensorsDo`):
две секунды стика от упора к упору не гасят ни одного пипса, а взмах над датчиком по той
же мысли — гасит.

На ПК (до железа): колесо мыши = динамо, стрелки = джойстик, **Q/A — датчик A, E/D —
датчик B**, Enter = зелёная кнопка, Esc = «в меню». Клавиша разгоняет значение датчика
(1.25 ед/с) и отпускается пружиной обратно (2.0 ед/с) — маппинг пакета, не наш; отгон на
клавиатуре = стучать по Q (нажал-отпустил = проход туда и обратно = два удара).

### Где живут значения панели (персистенс)

Статики хватало на переключение сценок, но не на закрытие редактора: основательница
оттюнила целую сессию, закрыла Unity — и получила дефолты. Поэтому **каждое**
изменение через панель (слайдер, тогглер, выбор варианта A/B/C, сворачивание
панели) пишет `TuningConfig` целиком в один файл, а стенд читает его обратно до
первой сценки.

| | |
|---|---|
| Файл в редакторе | `<projectPath>/UserSettings/tuning.json` |
| Файл в плеере | `Application.persistentDataPath/tuning.json` |
| Загрузка | `[RuntimeInitializeOnLoadMethod(BeforeSceneLoad)]` — один раз, до `PreviewMenu` |
| Сохранение | на каждое действие панели; temp-файл + `File.Replace` (обрыв записи не бьёт предыдущий файл) |
| «сброс к дефолтам» | `TuningStore.Clear()`: дефолты **и** удаление файла |

`UserSettings/` выбран сознательно: внутри проекта (легко найти и снести),
**вне `Assets/`** (нет импорта и `.meta`) и уже в `.gitignore` — сессия тюнинга
физически не может стать коммитом.

Формат — один плоский объект, ключ = имя поля `TuningConfig`, значение = число
или булево (enum пишется своим индексом). Поля находятся **рефлексией**: новый
`[tune]` начинает сохраняться в тот момент, когда объявлен, — про `TuningStore`
помнить не надо. Переименования безопасны в обе стороны: незнакомый ключ в файле
игнорируется, отсутствующий — берёт дефолт; несуществующий вариант enum'а тоже
откатывается к дефолту, а не превращается в мусорное значение. Отсутствующий или
битый файл = дефолты **молча**: правка настроек руками — не инцидент, ошибок в
консоль не летит (не-JSON, обрыв, вложенный объект, `NaN` — всё проверено
`TuningStoreTests`).

Тесты пишут в свой временный файл (`TuningStore.UseFileForTests`), а PlayMode-сьют
изолируется через `StandTestHarness.IsolateTuningFile()` — прогон сьюта не имеет
права переписать числа основательницы.

### Кнопка «в меню» и выход из игры

Один вход — `MenuButtonExit` (подписка на `ArcadeInput.MenuButton.Pressed`), но
две разные развязки, и это принципиально.

> Выход делает ИМЕННО кнопка «Меню» пульта, а не красная: это два разных контрола
> пакета (`ArcadeInput.MenuButton` / `ArcadeInput.RedButton`), и игра всегда слушала
> первый. Подпись финала 2026-09-22 сначала назвала красную и была переписана в тот
> же день под кнопку «Меню» (`GameTexts.FinaleExit`); менялся текст, код — нет.
> Красная кнопка в игре не занята ничем.

**Игра.** `MenuButtonExit.ExitAction` = `GameFlow.ExitToLauncher()` — тот же
метод, которым кончается финал. Он зовёт `PreviewStandNav.RequestExit()`: если
хук `ExitRequested` подписан (лаунчер автомата), игра **уходит** — экран прогона
уничтожается, `GameFlow` выключается (`enabled = false`), ни один таймер этого
прогона не тикает за спиной у лаунчера. Никакого `Application.Quit` и никакой
загрузки сцен: чем занять экран, решает лаунчер (ARCADE_INTEGRATION_CONTRACT §5).

**Фолбэк — отдельный явный путь.** Если хук не подписан (редактор, standalone-
билд одной игры), уходить некуда, и игра возвращается на свой титул —
`PreviewStandNav.ReloadEntryScene(Game)`. Раньше это был единственный путь, то
есть «выход» на автомате перезапустил бы игру, из которой игрок только что
попросился выйти.

**Стенд.** `MenuButtonExit` без `ExitAction` → `PreviewStandNav.ExitToScene()`:
`ExitRequested` + `LoadScene` в режиме Single. Из сценки это возврат в меню
стенда, из меню — чистая перезагрузка. Стенд — инструмент разработки, на стойку
он не едет.

Ни `Application.Quit`, ни `DontDestroyOnLoad`, ни переживающего статического
состояния (кроме `TuningConfig` с его файлом — это состояние стенда, не игры),
поэтому повторный запуск entry-сцены = чистый новый старт.

Тесты держат оба пути раздельно: основной кейс подписывает подставного лаунчера
(`GameTestHarness.PretendLauncherIsListening`) и требует именно выхода — с шести
точек потока и с финала; фолбэк — свой кейс без подписки, требующий чистого
возврата на титул.

## 4. Как гонять тесты

**В открытом редакторе:** Window → General → Test Runner, вкладки EditMode и
PlayMode.

**Headless (редактор должен быть закрыт — иначе editor lock):**

```bash
cd projects/meditation/components/unity-game
U=/Applications/Unity/Hub/Editor/6000.5.3f1/Unity.app/Contents/MacOS/Unity

# правила механик, контракт ввода (быстро)
"$U" -batchmode -projectPath "$(pwd)" -runTests -testPlatform EditMode \
     -testResults /tmp/editmode.xml -logFile /tmp/editmode.log

# реальные сцены стенда, оба «руками» через FakeBackend
"$U" -batchmode -projectPath "$(pwd)" -runTests -testPlatform PlayMode \
     -testResults /tmp/playmode.xml -logFile /tmp/playmode.log

# пересборка сцен, если их удалили
"$U" -batchmode -projectPath "$(pwd)" -quit -logFile - \
     -executeMethod Meditation.EditorTools.GameSceneBuilder.Rebuild
```

Exit code 0 = зелёно; XML содержит `total/passed/failed`.

### Что именно проверяют тесты

- **EditMode** — правила без сцены: кручение и grace, режимы срыва A/B, влияние
  скорости, детект удара против плавного наклона, прочность и затухание,
  таргетинг A/B/C, % перекрытия, волны и передышка, победа/поражение, взгляд,
  живучесть значений тюнера, контракт ввода, **персистенс панели**
  (`TuningStoreTests`: круговая сохранение→загрузка по всем полям сразу
  рефлексией, битый/отсутствующий файл → дефолты без ошибок, незнакомый ключ
  игнорируется, «сброс» удаляет файл, путь лежит в `UserSettings` вне `Assets`).
- **PlayMode игры** (`GameFlowTests`) — реальная `Game.unity`, обе руки через
  `FakeBackend`: старт по двум оборотам (и НЕ по одному), титул стоит картинкой дропа
  с кнопкой на измеренном месте и баром, который следит за оборотами; каждый уровень
  грузится со своим артом и деталями на позициях превью (±20 px, по нарисованному
  прямоугольнику) и со своим звуковым треком; обучение уровня 1 по трём битам
  (НАВОДИ → КРУТИ РУЧКУ + ТАЩИ → стрелка без текста; мысли не спавнятся,
  мысль накрывает деталь, сбор заблокирован), и то, что оно НЕ повторяется после
  поражения и возвращается на новом прогоне; победа по всем N деталям — бит награды,
  а поверх него готовый экран; оба поражения, стирание готового экрана оборотами и
  авто-ретрай; весь прогон титул → финал через все пять уровней; панель и её значения
  через прогон; «в меню» с шести точек потока и **мгновенная тишина всех трёх слоёв**;
  ни одна выведенная строка не рисуется ни на одном экране; луч доходит до собственного
  материала каждой детали и гаснет на пике; полоса наполнения нарисована и растёт на всех
  пяти уровнях; радиус прицела двигает и круг, и попадание одним слайдером. Отдельно про офис: он обязан предлагать
  РОВНО пять целей (жёлтые стикеры в плите — обманки, шестая цель сделала бы уровень
  непроходимым) и его слой мыслей обязан закрывать полосу переднего плана, где лежат
  тапки и стоит кружка, оставаясь под HUD. Чистый выход проверяется не именем сцены,
  а тем, что игра снова стоит на титуле с одним экраном за спиной.
- **PlayMode стенда** — **реальные сцены**: каждая грузится и не сыплет ошибок в
  консоль; раскладка сверяется с числами SCREENS.md через
  `DesignStage.DesignRectOf` (видимый прямоугольник, а не `activeSelf`);
  обе руки играются через `FakeBackend`; кнопка «в меню» действительно
  возвращает в меню; значения тюнера переживают смену сценок **и «закрытие
  редактора»** (двинуть экранный слайдер → файл появился → обнулить всю статику →
  прочитать файл → значение вернулось).
- **Скриншоты игры** — `GameScreenshotTests` играет обязательный набор
  (титул, все пять карточек, L1 интро/обучение×3/игра/пик/победа, L2…L5 в игре, L3
  с видимым оверлеем сосуда, победы уровней, поражение + ретрай, финал) плюс кадры
  этого дропа: готовый экран победы, проход луча, неон-обводка, рост мыслей, прицел
  на трудных плитах и два крупных плана ободка — `Game01…Game48`. С дропа 2026-09-25
  туда входят шесть кадров ПРЯТОК (42–47, по паре «покой → захват» на акулу, гуся и
  окно со шторой) и 48 — то же окно крупно уже после сбора шторы; их снимает
  `GamePictureTests`, потому что каждая пара — ещё и пиксельный замер (см. ниже).
  Перечень с расшифровкой — в шапке самого класса. Кадры ждут не «N
  кадров», а того, что затемнение перехода реально сошло: batch рисует сотни
  кадров в секунду, и первый финал вышел на три четверти чёрным, пройдя при этом
  все проверки.
  **Кадры игры снимаются в ОТГРУЖАЕМОМ режиме выбора детали** (`NoticeMode.GazeJoystick`),
  с припаркованным кругом (`GameTestHarness.NoticeByLooking` / `ParkTheAim`). До
  2026-08-08 почти весь набор ставился в `FixedOrder` — вариант, который прицел
  ВЫКЛЮЧАЕТ, — и founder судила заказанную ею фичу по одному кадру из пятидесяти, да и
  тот был кругом над пустым небом. Круг ПАРКУЕТСЯ, а не ведётся стиком: стик двигает его
  со скоростью в px/с, и тест, летящий им через плиту, садится туда, куда его занесёт
  частота кадров; парковка — это то же самое, что делает рука игрока
  (`GazeSelector.Position` и есть «где прицел»), а всё дальнейшее — удержание, хит-тест,
  рисунок — уже игра. `FixedOrder` остался там, где кадр не про выбор детали вовсе (луч,
  победы, поражение).
  **Ожидания кадров — по видимым пикселям, а не по модели.** Кадры волн ждали
  `Thoughts.Count`, и гейт 2026-08-08 получил два «кадра волн» с 0.67 % чернил против
  0.66 % на ПУСТОМ обзоре: мысли влетают с краёв, поэтому три штуки — это регулярно три
  полоски за кромкой. Теперь ожидание идёт по закрашенной доле
  (`GameTestHarness.PaintedShare`: видимая часть прямоугольника × запечённая доля чернил
  спрайта), а само утверждение снимается с написанного PNG — слой мыслей гасится, и
  считаются сдвинувшиеся пиксели. Тем же способом кадр доказывает, что на нём виден
  прицел и что полоса наполнения не ушла под мысли.
  **Пик хаоса ставится ЖИВЫМ потоком.** Кадры 07 и 24 набирались вызовом, который
  раскладывал 15 мыслей сеткой 5×3 с шагом ячейки, — композиция, которой игра не
  выдаёт ни при каких настройках, и гейт судил по ней. Теперь
  `GameTestHarness.CrowdTheScreenByPlaying` заводит волны ручками панели и просто ждёт,
  пока перекрытие дойдёт до порога, а `AssertNotAGrid` не даёт кадру снова стать
  решёткой: у сетки центры мыслей стоят по колонкам и рядам с точностью до пикселя
  (5 колонок на 15 мыслей), у живого наплыва — все свои. Сеточный
  `CrowdTheScreen` остался там, где нужен только ЧИСЛЕННЫЙ порог (переключился ли
  флаг пика, поменялись ли слои), и помечен как «никогда для кадра».
- **Претензии к КАРТИНКЕ** (`GamePictureTests`) — считают пиксели на том же
  рендере, которым снимаются кадры дизайн-гейта (`StandTestHarness.Capture`).
  Три находки гейта нельзя было поймать арифметикой до отрисовки, и они не были
  пойманы: силуэт в слоте «нечитаем» только скомпонованным на своей плашке (гард
  ink ≥ 8 % и контраст ≥ 90 снят 2026-08-08 вместе с самим рядом слотов — гард
  читаемости невидимого виджета бывает только зелёным); добыча «вылезла из
  сосуда» только против его СИЛУЭТА — сосуд
  снимается один на пустом фоне, и каждая ячейка добычи обязана лежать в нём на
  100 %; десатурация поражения была зелёной в сьюте ровно потому, что тест мерил
  палитру greybox-стенда, через которую игра не проходит. Это же и есть страховка
  от грабель «зелёный тест — сломанная картинка».

  Гейт **поражения** переписан дважды, и оба раза потому, что мерил величину, которой
  к моменту прогона уже не было. Сначала — полоса насыщенности 0.07–0.26, снятая с
  бирюзовых силуэтов: дроп 2026-08-05 сделал её не «неверной», а неизмеримой (у чёрной
  штриховки насыщенность 0 по построению). Потом — контраст строки «Мысли захватили
  всё. Вдохни.» к своей подложке: эта строка выведена из игры, а предложение на экране
  теперь пиксели, которые набрала дизайнер. Осталось то, за что отвечает поток:
  отрисованный экран действительно встал (кадр отличается от кадра уровня),
  действительно не чёрный (95-й процентиль яркости ≥ 0.035 при 0.072 у самого рендера
  и 0 у пустого кадра) и действительно стирается оборотами — пять оборотов меняют
  ПИКСЕЛЬ, а не только счётчик.
  Ряд заполненных слотов на победе с этого дропа не снимается: его больше нет, а
  проверка «добыча внутри силуэта сосуда» стала важнее, а не менее важной — бит
  награды теперь единственное место, где игрок видит собранное.

  **Прятки (2026-09-25)** проверяются здесь же и по той же причине: «полный арт
  РЕАЛЬНО виден» — утверждение про то, сколько кадра красит рисунок, и подмена
  спрайта в прямоугольник неправильного размера прошла бы любую проверку состояния,
  ничего не показав. `EveryHideaway_ShowsWhatItHides_OnlyWhileItIsHauled` берёт по
  два кадра на прятку и меряет `ShareCoveredBy` самого `Image` детали: акула обязана
  красить ≥ ×3.0 от плавника (замер ×6.0), гусь ≥ ×1.35 от головы (замер ×1.63 —
  его фрагмент уже почти гусь, поэтому потолок низкий и порог назван по каждой
  прятке отдельно), а «дама за шторой» — < 2 % своего прямоугольника в покое и
  > 50 % при захвате. **Negative control обязателен:** соседняя ОБЫЧНАЯ деталь того
  же уровня меряется в тех же двух кадрах и обязана красить столько же (±15 %), иначе
  замер ловит не прятку, а волну, пульс или луч — все три на этих кадрах выключены,
  и контроль это и доказывает. Отдельно: `AHideawayThatIsDropped…` снят на
  ОТГРУЖАЕМЫХ grace и режиме остановки (300 мс, сброс в ноль), а
  `TheWomanBehindTheCurtain_StaysUncovered…` держит единственное состояние, к
  которому цикл сбора больше не возвращается.

  **Подписи поверх готовых рендеров** (`AssertACodeCaptionStandsOffTheRender`,
  в `GameScreenshotTests`): две строки в игре наши на чужой картинке — «Крути
  крутилку, чтобы начать» и «Жми кнопку Меню…». Их читаемость — свойство РЕНДЕРА,
  которое переголосовывается каждый раз, когда Катя присылает новый (дроп 2026-09-25
  прислал оба). Гард гасит подпись, снимает тот же кадр ещё раз и сравнивает светлые
  пиксели букв с тем, что картинка кладёт ровно в эти места: порог — 60 из 255.

  **Мысли на тёмных плитах — гейт вывернут наизнанку 2026-08-08.** Он мерил, что
  ореол несёт свой свет: медианная яркость рядом со штрихом, легшим на тёмное
  место плиты (< 2.4:1), ≥ 0.30 — замер 0.871 на всех пяти уровнях, негативная
  проверка с выключенным ореолом 0.232…0.247, все пять красные. Число осталось
  верным, но перестало быть про игру: ореола по умолчанию нет. Теперь
  `EveryLevelsThoughts_AreBlackInk_AndTheHaloIsAToggle` держит два утверждения
  сразу — (1) как отгружается: слой мыслей ЗАТЕМНЯЕТ ≥ 5000 px и высветляет
  меньше пятой части от этого (белого на штрихах нет; пятая часть — запас на
  диски пипсов); (2) негативный контроль: включённый тогглер ВИДИМО светлеет тот
  же кадр (≥ 5000 px, света вдвое больше тени; хуже всех метро — 3.2×). Без
  второй половины «подложки нет» удовлетворил бы и молча не загрузившийся шейдер,
  и founder сравнивать было бы нечем при зелёной сьюте. Первую формулировку
  прежнего гейта (локальный контраст штриха где угодно на кадре) пришлось
  выбросить ещё тогда: она проходила и с выключенной подложкой, потому что рядом
  со штрихом почти всегда есть светлое место плиты.
- **Скриншоты стенда** — `StandScreenshotTests` не просто снимает пять стартовых
  экранов: он **играет** нужные состояния (сбор на полпути, срыв, деталь в
  сосуде, погашенные пипсы, крепкая L-мысль, конфликт двух рук, пик перекрытия,
  победа, поражение) и пишет их в `Screenshots/*.png`
  (gitignored). Пропавший или пустой кадр — красный тест: дизайн-гейт судит по
  состояниям, а стартовый кадр про игру о двух руках не доказывает ничего.

## 5. Известные упрощения

- Упаковка игры как UPM-пакета (`ARCADE_INTEGRATION_CONTRACT` §2: своя корневая
  папка + `package.json` + `game.json`) ещё не сделана — стенд лежит в
  `Assets/_Project/`. Ввод, жизненный цикл и «в меню» уже по контракту, так что
  переезд в пакет — механический шаг следующего инкремента.
- Панель тюнеров управляется мышью: это инструмент разработки, на стойку она
  не поедет.
- Сцена стенда собрана по СТАРОМУ greybox-уровню 1 «Деловой квартал» —
  прямоугольники и палитра из walkthrough. Это уже не композиция игры (та живёт
  в `LevelCatalog`), и так и задумано: стенд судит механику, а не картинку.
- **HUD уровня стоит на пер-уровневых координатах, а не на константах SCREENS.**
  Индикаторы SCREENS фиксирует по координатам, детали фиксирует превью, и они
  пересекались в пяти точках; founder решила (2026-07-31): **двигается HUD, арт
  не трогаем**. Поэтому `LevelDefinition` носит `CrankIndicatorCentre`
  рядом с деталями и сосудом (`SunCentre` ушёл 2026-08-07, `SlotsOrigin` —
  2026-08-08 вместе с рядом слотов; четыре из шести сдвигов таблицы SCREENS
  существовали ради ряда и выведены вместе с ним), а
  `LevelView` рисует из них; `StageView` (greybox-стенд) остался на базе
  SCREENS, и его layout-тесты по-прежнему сверяются с ней. Уровни 1–2 прошли
  через тот же тест: на набережной не подвинулось ничего, в офисе индикатор
  динамо ушёл вправо по полу (140 → 375), потому что под ним лежали тапки.
  Клиренс — свойство КАТАЛОГА, а не отрисованного кадра: он решается
  арифметикой до всякой сцены, поэтому его держит EditMode-тест
  (`LevelCatalogTests.NoHudWidget_Overlaps*`), а не глаз на скриншоте, который
  заметил бы это, только посмотрев в нужный угол нужного уровня. Тест меряет
  то, что реально нарисовано (индикатор по гало r=86, а не по дуге r=70, плюс
  полоса наполнения сосуда), требует остаться в своём углу и не уехать от базы дальше
  необходимого — и потребует позиций у уровней 1–2, когда их соберут.
  Два сдвига из пяти чинили не композицию, а проходимость: воробей L5 и плакат
  L3 целиком лежали под непрозрачным рядом слотов — деталь, которую нельзя
  увидеть и на которую нельзя навести взгляд, при победе «все N деталей». Оба
  сдвига отменены 2026-08-08: ряда нет, и закрывать эти детали больше нечем.
