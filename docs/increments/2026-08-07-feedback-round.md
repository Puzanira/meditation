# Increment: правки по переписке с дизайнером + решения founder

Дата: 2026-08-07. Источник: разбор старой переписки founder↔дизайнер +
решения founder в сессии (уровни не переставлять; мысли только разрастаются;
таймер убрать; отгон на датчики — отдельный инкремент с гейтом).

## Состав (быстрый инкремент, внутренние гейты без визита founder)

1. **Обучение не повторяется после поражения:** рестарт уровня 1 после
   проигрыша — сразу в игру (интро/биты пропускаются). Первый заход — как был.
2. **Таймер УБРАН (решение founder: «давай уберём, он плохо работает»):**
   солнце-циферблат и подпись уходят из HUD всех уровней; победа = все детали
   собраны (без лимита времени); поражение = только заполнение экрана
   мыслями; параметры длительности уровней и тогглер «таймер в обучении
   стоит» выводятся из панели; MECHANICS §5/§6 и SCREENS — supersede записью
   (старые правила не стирать молча, пометить решением 2026-08-07).
3. **Рост мыслей со временем:** мысль спавнится своим классом (S/M/L) и
   растёт со временем жизни — эффект «разрастаются и заполняют экран».
   [tune]: скорость роста %/с и потолок роста ×; на панель, per level.
   Прежний инвариант «размер жёстко зажат классом» superseded этим решением —
   тесты классов переводим на «начальный размер = класс, рост ≤ потолка».
4. **Волны:** L1 (набережная) — мыслей больше (состав/интервал вверх);
   L2 (офис) — количество ниже L1, но размер-класс сдвинут вверх («чуть
   больше первого»). Конкретику дефолтов подобрать и показать founder кадрами;
   лестницу прогрессии в тестах переписать под новую кривую.
5. **Неон-прицел:** круг взгляда крупнее и заметнее, неон-стилизация
   шейдером (в духе арта; параметры на панель).
6. **Прогресс-бар под сосудом** — на всех уровнях (метро-оверлей остаётся).
7. **Неон-обводка деталей [toggle]** — кодовая обводка по альфе спрайта
   (без отрисовки арта), тогглером на панель, чтобы founder сравнила с
   пульсом и лучом и выключила лишнее.

Не трогать: луч (доделывается параллельной задачей), позиции/составы уровней,
готовые экраны, звук-модель.

## Approved by founder

2026-08-07, терминал: решения продиктованы в сессии (см. шапку); состав
собран Maintainer'ом из переписки + решений. Быстрый инкремент — внутренние
гейты (suite, Codex, дизайн-скептик), комплект founder смотрит разом.

## Checkpoint log

- 2026-08-07: файл создан; запуск owner'а после завершения параллельной
  задачи «луч» (одно дерево — один владелец).
- 2026-08-08: инкремент сделан целиком, все 7 пунктов. База — 0474820.
  Suite GREEN (exit 0): EditMode 126/126, PlayMode 148/148, прогон
  `.studio/runs/suite-20260808-*`; два предыдущих прогона красные (8 падений
  → 0), см. ниже.
  - **п.1** обучение помнит `GameFlow`, а не экран: флаг ставится при ВХОДЕ в
    уровень 1 (проигравший во время обучения уже обучен) и сбрасывается на
    титуле, а не в `StartRun` — на титул ведут все три дороги. Рестарт У1
    пропускает и обзор.
  - **п.2** таймер убран из правил, из HUD игры И со стенда, из всех панелей;
    MECHANICS §5 переписан supersede-блоком (старая редакция оставлена целиком),
    §4/§6 и SCREENS «Зоны»/«Обучение»/«Состояния» — записями со ссылкой на
    решение 2026-08-07.
  - **п.3** `Thought.SpawnSize` (класс) × `GrowthScale` (clamp 1…потолок);
    зажим снизу единицей — прямое слово founder, у него свой тест. Обои
    поражения не растут.
  - **п.4** У1 5.0 с / 2 слабых; У2 5.0 с / 1 средняя. У3–У5 не тронуты.
    Лестница в тестах переписана на давление = площадь классов волны / интервал.
  - **п.5** `Meditation/GazeNeon`, радиус [tune] 130 (было 90), три ручки.
  - **п.6** полоса наполнения на всех пяти уровнях, метро не тронуто; прижим
    к кадру ради рюкзака библиотеки.
  - **п.7** `Meditation/DetailOutline` [toggle], по умолчанию ВЫКЛ, две ручки.
  - Кадры для founder: весь контракт переснят (солнце пропало отовсюду) плюс
    новые **33** (неон-обводка ВКЛ, пара к 09) и **34** (разросшиеся мысли).
  - Открытый вопрос к founder: на У4 (библиотека) сосуд-рюкзак сам вылезает за
    нижний край кадра, поэтому полоса наполнения ложится ему на низ, а не под
    него. Оставлено так; альтернатива — поднять полосу НАД рюкзаком.
- 2026-08-08: **фикс-раунд по ревью Codex объединённой дельты.** Две находки
  били ровно в этот инкремент — п.3 (мысль не меньше класса) и п.4 (лестница
  прогрессии), — и обе оказались настоящими.
  - **Блокер 1 — п.3 нарушался этажом ниже собственного теста.** `Thought.SpawnSize`
    брал `Min(силуэт, класс)`, а `ArtLibrary.FitThought` вписывал спрайт ВНУТРЬ
    коробки, поэтому узкие силуэты спавнились сильно меньше класса: бутылка У1
    (холст 454×1521) в классе S — **42×140** вместо 180×140, гитара — 54×140.
    Тест «не меньше класса» этого не видел: он спрашивал сам фит, а не то, что
    спавнится. Теперь фит **cover** (обе стороны ≥ сторон класса), `SpawnSize`
    берёт `Max`, а тест — `EveryThoughtSilhouette_SpawnsNoSmallerThanItsSizeClass`
    — прогоняет ФАКТИЧЕСКИЙ спавн каждого спрайта каждого уровня во всех трёх
    классах. Новые размеры узких: бутылка **180×603** (S) / 280×938 (M) /
    420×1407 (L), гитара **180×471** / 280×733 / 420×1099, банкнота У2
    **261×140** / 411×220 / 597×320.
  - Раз силуэт теперь торчит за коробку, **перекрытие считается по чернилам, а
    не по прямоугольнику**: `Thought.Ink` = доля холста, которую спрайт реально
    закрашивает (замер по 25 PNG: 0.15 у вопросов У5 … 0.64 у банкноты У2),
    `OverlapPercent` перемножает слои — клетка под k мыслями закрыта на
    1 − Π(1 − ink). Грейбокс-стенд и обои поражения имеют ink = 1, поэтому всё,
    где арта нет, считается как раньше. Запечённые доли перемеряются по самим
    PNG (`EveryThoughtsInkShare_MatchesItsPng`).
  - **Блокер 2 — п.4 зеленился на неверной арифметике.** Давление меряли
    площадью КЛАССОВ; по фактическим размерам шипнутая лестница на первой же
    ступени шла вниз (У1 23 695 px²/с против 17 318 у У2). Плюс при нулевом
    отгоне **У1 и У2 нельзя было проиграть** — на У1 перекрытие вставало на
    полку около 49 %. Составы подняты: **У1 5.0 с / 3 слабых + рост давления
    4 %** (был 0), **У2 4.5 с / 2 средних**. Лестница по фактическим размерам:
    35 542 → 38 483 → 70 178 → 104 168 → 137 421 px²/с. Заказ founder держится:
    У1 шлёт БОЛЬШЕ мыслей (3 > 2), класс У2 выше (M против S). У3–У5 не тронуты.
  - Новый тест `EveryLevel_FillsUpAndIsLost_WhenNobodyBeatsAThoughtOff`: модель
    уровня 3 минуты при НУЛЕВОМ отгоне, каждый уровень обязан дойти до порога
    поражения. Бюджет заведомо щедрый — он охраняет ПРАВИЛО, а не цифру
    баланса; фактические времена печатаются в отчёт теста.
  - Doc drift п.2: GAME_SPEC «на уровень отведён таймер» / «до истечения
    времени» → supersede-врезка; кандидат MVP лишился таймера; карточка сценки
    4 на стенде («таймер, победа, поражение») → «победа, поражение по
    заполнению». SCREENS «рамка, в которую вписывается спрайт» → «рамка, которую
    спрайт покрывает».
  - **Вопрос к founder (баланс, не код):** У1 теперь проигрывается за ~2 мин
    полного бездействия и получил рост давления 4 %. Это минимум, при котором
    обучающий уровень вообще имеет поражение; если ощущается жёстко — ручки
    «интервал волн», «состав волны» и «сокращение интервала за волну» есть на
    панели per level.
- 2026-08-08: **вердикт дизайн-скептика по объединённой дельте — «вернуть», 8
  находок (F1–F8)**, отданы агенту раунда 2: F1 прицел есть только в одном
  кадре (остальные сняты в FixedOrder, который его выключает) — базовые кадры
  перевести на отгружаемый GazeJoystick + кадры на метро/библиотеке + поверх
  детали; F2 кадры волн без волн (слитеры за краем, перебитые бэнды) — снимать
  на отгружаемых дефолтах, ждать по видимой площади; F3 Game34 без «свежей
  рядом» (за краем); F4 обводка: заливает тонкие детали (dilate 6 px смыкает
  просветы скрепки) и режется по UV-краю (тапки) — адаптивная дилатация +
  padding; F5 прогресс-бар на VesselLayer, исчезает под мыслями на пике — в
  HUD-слой по SCREENS; F6 стрелка отгона растёт из пипсов и обрывается в небе
  — зазор от полного bbox, кончик к фикс-точке у нижней кромки по центру;
  Game29 переснять позже момента; F7 walkthrough не догнал решения (солнце,
  «тряси», r=90) — supersede-синхронизация + MECHANICS:247 + GameTexts.cs:20;
  F8 коммуникация: «реже» на L2 = меньше мыслей в волне и класс выше
  (интервалы 5.0/4.5). Вкус founder (в её гейт-лист): пипсы/ореол — константные
  px у разросшихся мыслей; бар L4/L5 поднять/оставить; стрелка от фикс-точки.
