# Экраны и сцена — детальный спек (greybox)

Статус: утверждено founder'ом. Арт — прямоугольники-плейсхолдеры, но
**раскладка, размеры и поведение — обязательные**: реализатор не изобретает
layout, а собирает по этому доку и единому визуальному мокапу
**`docs/design/gameplay-walkthrough.html`** — вся игра по порядку в 24 кадрах
(титул → карточка → обучение → цикл сбора → пик хаоса → поражение/победа →
сцены уровней 2–5 → финал → раскладка зон) + реестр всех игровых текстов.
На каждом геймплейном кадре — панель состояний рук (динамо/джойстик).

Разрешение экрана стойки: **1920×1080** (подтверждено founder'ом 2026-07-29).
Все координаты в px от левого-верхнего угла, без масштабирования.

**Источник композиций сцен — раскадровки founder'а**
`docs/files (1)/Uroven_1_l1_raskadrovka.png … Uroven_5_l5_raskadrovka.png`.
Кадры 1–6 каждой раскадровки соответствуют состояниям уровня: обзор (intro) →
перетаскивание (play) → первая мысль → пик хаоса (peak) → отгон → финал (win).
Координаты ниже — перенос раскадровок на сетку 1920×1080, допуск ±50 px.

Поток экранов:

```
Лаунчер (удержание джойстика)
  → S1 Титул → S2 Карточка уровня → S3 Уровень (L1 = с обучением)
       S3 победа → S4 Победа уровня → S2 следующего уровня … → S6 Финал → лаунчер
       S3 поражение → S5 Поражение → рестарт того же S3
Кнопка «в меню» из ЛЮБОГО экрана → мгновенный чистый выход (контракт стойки).
```

---

## S3. Экран уровня — главный (описан первым, он определяет всё)

### Зоны (постоянная раскладка, все уровни)

| Зона | Прямоугольник (x, y, w, h) | Что в ней |
|------|---------------------------|-----------|
| Сцена | 0, 0, 1920, 810 | фон локации + детали уровня на авторских позициях (по `превью.png`) |
| Полоса переднего плана | 0, 810, 1920, 270 | земля/пол, на ней сосуд |
| Сосуд | позиция и габарит — авторские, per уровень (см. сцены уровней) | цель сбора; собранные детали видны внутри |
| HUD: слоты деталей | от (60, 40): N слотов 72×72, шаг 88; N = число деталей уровня (L1 10, L2 8, L3 7) | силуэты деталей уровня; собранная — заполняется |
| HUD: таймер | круг, центр (1800, 100), r=60 | «солнце»-циферблат: сектор убывает по часовой |
| HUD: индикатор динамо | круг, центр (140, 950), r=70 | стрелка скорости кручения; зелёная дуга = «достаточно» |
| Слой мыслей | поверх сцены и деталей | мысли закрывают обзор; HUD они не закрывают |

Z-порядок (снизу вверх): фон сцены → детали → нить сбора → слой мыслей →
полоса переднего плана и сосуд *(закрывается ли сосуд мыслями — [toggle],
MECHANICS §4)* → HUD.

### Детали в сцене

- Габарит детали: обычно 40–120 px по большей стороне (мелкая, «интересная»);
  вытянутые исключения (лоза L1) — по превью.
- Позиции авторские, на уровень фиксированы, источник — `превью.png` каждого
  уровня (арт-дроп 2026-07-31). Таблицы координат — раздел «Сцены уровней»
  ниже; координаты приблизительные (±20 px), при расхождении авторитет —
  превью.

- Незамеченная деталь слегка пульсирует: масштаб 1.0→1.12, цикл 1.6 с
  *(заметность пульса **[tune]** — на поздних уровнях слабее)*.

### Выбор детали («заметить») — конкретный дизайн вариантов [toggle]

Базовый вариант — **B (взгляд джойстиком)**, A и C остаются тогглерами:

- **B. Взгляд джойстиком (базовый).** Плавный наклон стика двигает по сцене
  мягкий круг-«взгляд» r=90 px, скорость 900 px/с **[tune]**. Взгляд держится
  на детали 0.4 с **[tune]** → деталь «замечена»: вспышка + от детали к сосуду
  протягивается пунктирная нить. Дальше сбор ведёт динамо, взгляд не нужен.
  Разводка жестов одного стика: плавный наклон = взгляд; резкие смены
  направления (детект удара, MECHANICS §3) = тряска. Во время тряски взгляд
  замирает на месте.
- **A. Фиксированный порядок.** Взгляда нет; текущая цель подсвечена пульсом,
  кручение тянет её. Порядок авторский (от заметной к спрятанной).
- **C. Автовыбор.** Кручение тянет ближайшую к сосуду деталь, не закрытую
  мыслями.

### Сбор (визуал состояния)

- Замеченная деталь едет к сосуду по нити со скоростью = прогресс кручения;
  путь = прямая деталь→сосуд.
- Вокруг едущей детали — **кольцо прогресса**: заполняется по мере пути;
  пока динамо крутится выше порога — кольцо зелёное; кручение упало — кольцо
  краснеет, и через grace-период деталь «срывается»: летит обратно на своё
  место (0.6 с), кольцо гаснет (или тает — режим B, MECHANICS §1).
- Индикатор динамо (HUD слева-внизу) дублирует состояние: стрелка в зелёной
  дуге = хватает, вышла — красная подсветка + вибрация иконки.
- Деталь дошла до сосуда: подпрыгивание сосуда (scale 1.0→1.1→1.0, 0.3 с),
  деталь появляется внутри сосуда, слот в HUD заполняется.

### Мысли (визуал состояния)

- Габариты: S 180×140, M 280×220, L 420×320 px (скруглённые прямоугольники-
  плейсхолдеры с подписью типа мысли).
- Спавн: выплывают из-за краёв экрана (случайная сторона), дрейф к центру
  20–60 px/с **[tune]**; перекрываются.
- Прочность: под мыслью ряд точек-пипсов (= числу оставшихся ударов).
  Удар тряски: мысль вздрагивает (±6 px, 80 мс), пипс гаснет. Последний пипс —
  мысль лопается (масштаб 1.1 → 0, 200 мс).
- Пик хаоса: при перекрытии ≥ порога **[tune]** — лёгкое затемнение краёв
  экрана + учащение «сердцебиения» фона (см. Audio, позже).

### Состояния S3 (машина состояний уровня)

1. `intro` (2 с): сцена без мыслей, все детали пульсируют разом — «обзор».
2. `play`: основной цикл (MECHANICS §1–5). Таймер идёт.
3. `peak`: перекрытие ≥ порога — визуал пика хаоса (не отдельная логика).
4. `win` → S4; `lose` → S5.

### Обучение (только уровень 1, скриптовые биты внутри `play`)

1. Мысли не спавнятся. Стрелка + текст у первой детали: «Крути ручку —
   собери одуванчик». Таймер стоит.
2. Первая деталь в сосуде → спавнится первая мысль (плюшевый мишка), сбор
   заблокирован её появлением поверх следующей детали. Текст: «Тряси
   джойстик — отгони мысль!»
3. Мысль отбита → таймер запускается, дальше обычный `play`.
   *(Таймер в обучении стоит — [toggle: да/нет].)*

---

## Сцены уровней (1–3 активны; 4–5 отложены)

Общие правила: сцена 0–810 по вертикали, полоса переднего плана 810–1080;
позиции — центры объектов. **Решение (2026-07-31): арт-дроп = канон.**
Сосуд больше НЕ фиксирован в центре — позиция авторская per уровень (по
превью); нить сбора тянется к фактической позиции сосуда. Координаты ниже
сняты с `превью.png` (±20 px), при расхождении авторитет — превью.

### Уровень 1 — Улица, вечер · сосуд: портфель на тротуаре, центр (1060, 935), ~290×170

Фон: `L1/background-only.png`. Сосуд: `objects/briefcase.png`.
Таймер-солнце (1800, 100) r=60 — поверх фона; луна-деталь в (1670, 110)
рядом, не пересекаются. 10 деталей:

| Деталь | Файл | Позиция (центр) |
|--------|------|-----------------|
| Воробей на крыше | bird.png | (180, 80) |
| Облако у столба | cloud-middle.png | (720, 50) |
| Облако у провода | cloud-2.png | (1375, 150) |
| Луна | moon.png | (1670, 110) |
| Летучая мышь на проводе | bat.png | (1505, 220) |
| Шторы в окне | curtains.png | (820, 390) |
| Горшок с цветком в окне | flowerpot.png | (1250, 620) |
| Лоза на стене | vine.png | (1585, 630) — вытянутая, ~190×990 |
| Одуванчик в асфальте | dandelion.png | (495, 975) |
| Лужа у двери «Pipo» | puddle.png | (1440, 990) |

### Уровень 2 — Библиотека · сосуд: рюкзак на полу, центр (555, 945), ~175×190

Фон: `L2/Level2_Background_Plate.png` (стеллажи, постер, окна, мебель
запечены). Сосуд: `objects/Backpack.png`. 8 деталей:

| Деталь | Файл | Позиция (центр) |
|--------|------|-----------------|
| Солнце на постере | Sun_Circle.png | (995, 170) |
| Кот на подоконнике | Cat.png | (1570, 620) |
| Бюст | Bust.png | (1205, 640) |
| Раскрытая книга на столике | Open_Book.png | (780, 780) |
| Лампа на столике | Lamp_Desk3.png | (870, 780) |
| Очки на скамье | Glasses.png | (1620, 860) |
| Валентинка на столике | Valentine_Card.png | (190, 865) |
| Мышь с книжкой на полу | Mouse_With_Book.png | (85, 1020) |

### Уровень 3 — Метро · сосуд: сумка пассажира (запечена в фон), центр (1260, 945)

Фон: `L3/Level3_Background_clean.png` — пассажир и сумка-сосуд запечены.
Наполнение сосуда: отдельный спрайт сумки или оверлей-индикатор поверх
(запрошен у дизайнера; до тех пор — оверлей на превью-стенде). 7 деталей:

| Деталь | Файл | Позиция (центр) |
|--------|------|-----------------|
| Гусь заглядывает слева | goose.png | (95, 455) — торчит из-за края |
| Плакат «Пляжи Сызрани» | ad_poster.png | (745, 70) |
| Стикер на поручне | sticker.png | (1570, 375) |
| Газета в воздухе | newspaper.png | (885, 515) |
| Перчатка на сиденье | glove.png | (395, 660) |
| Лужа на полу | puddle.png | (170, 905) |
| Рыба на полу | fish.png | (1740, 1000) |

### Уровень 4 — Набережная (Питер) · сосуд: ведёрко 180×140

> **Отложено (решение founder 2026-07-31):** уровни 4–5 пока не делаем —
> арта нет. Спека ниже сохранена на будущее (greybox-прямоугольники).

Фон горизонтальными полосами: небо (0, 0, 1920×360); вода (0, 360, 1920×330);
чугунная решётка вдоль воды (0, 690, 1920×40); гранит набережной
(0, 730, 1920×80) + передний план. Волна-дуга у левого края воды (380, 470).

| Деталь | Позиция |
|--------|---------|
| Чайка | (1000, 190) — в небе |
| Кораблик на воде | (1420, 470) |
| Ракушка | (580, 680) — у кромки воды |
| Морской конёк на чугунной решётке | (1300, 710) |
| Мокрые следы на асфальте | (300, 770) — на граните |

### Уровень 5 — Офис · сосуд: кружка на столе, 160×140

Фон: стена; монитор (440, 280, 620×420) стоит на столе; окно с подоконником
справа (1280, 170, 510×440). Стол = полоса переднего плана (810–1080).

| Деталь | Позиция |
|--------|---------|
| Стикер-напоминалка | (900, 420) — на краю монитора |
| Растение на подоконнике | (1530, 390) — в окне |
| Скрепка причудливой формы | (130, 760) — на столе слева |
| Игрушка-антистресс | (480, 850) — на столе |
| Чьи-то тапки из-под стола | (1380, 980) — под столом справа |

*Примечание: детали уровня 5 частично лежат в полосе переднего плана — слой
мыслей должен закрывать и её (кроме HUD), иначе детали на столе не прячутся.*

---

## Арт-ассеты уровней 1–3 (дроп дизайнера, 2026-07-31)

Папка `docs/design/L1-2-3/` — первый арт-дроп: фоны 1920×1080, спрайты
объектов и мыслей для уровней 1–3. Стиль: плоский вектор, приглушённые
палитры; **мысли — единые бирюзовые мягкие силуэты** (один цвет на все
уровни). В каждой папке `превью.png` — авторская сборка сцены, это референс
расстановки объектов (заменяет прямоугольные позиции из таблиц выше).

> **Решение founder (2026-07-31): арт = канон.** Таблицы сцен уровней 1–3
> выше уже переписаны по превью. Правило: каждый отдельный спрайт из
> `objects/` = деталь для сбора (декор запечён в фон); сосуд L2 = рюкзак;
> недорисованные детали старой спеки (чашка, тень, перо, цветок, отражение,
> капля) выкинуты; наборы мыслей = арт; уровни 4–5 пока не делаем.

### Маппинг файлов

**L1 — улица (вечер).** Фон `background-only.png` (в фон запечены
надписи-пасхалки: DENNY WUZ HERE, B+K, Pipo, HELLO stranger).
Сосуд: `objects/briefcase.png` (портфель, стоит на тротуаре по центру).
Объекты: dandelion (одуванчик ✓ спека), bird (воробей ✓), cloud-2 +
cloud-middle (облака ✓, два), moon, bat, vine, curtains, flowerpot, puddle
(новые, в спеке не было). Из спеки отсутствуют: кофейная чашка, тень от
вывески.
Мысли (`мысли/`): teddy-bear ✓, empty-bed ✓, cake ✓, question-marks ✓,
coin-purse ✓ — 5 из 9 по WORLD.md.

**L2 — библиотека.** Фон `Level2_Background_Plate.png` (стеллажи, постер
«Kozemirych», окна; мебель запечена). Сосуд: `objects/Backpack.png` —
**рюкзак**, в спеке была тканевая сумка на стуле. Объекты: Open_Book ✓,
Glasses ✓, Sun_Circle (солнце-круг — вероятно замена «лучу света»?),
Valentine_Card, Cat, Mouse_With_Book, Lamp_Desk3, Bust (новые). Из спеки
отсутствуют: закладка-перо, засушенный цветок.
Мысли: AlarmClockLegs ✓, Burger ✓, BabyHeadTie, Mushroom, SpeechBubbleDots
(последние три — не из списка WORLD.md).

**L3 — метро.** Фон `Level3_Background_clean.png` — **пассажир и его сумка
(= сосуд) запечены в фон**, отдельного спрайта сосуда нет. Объекты: glove ✓,
sticker ✓, newspaper ✓, goose, fish, ad_poster, puddle (новые). Из спеки
отсутствуют: отражение в окне, капля на потолке.
Мысли: phone_call ✓, soccer_ball, exclamations, beetle, sock (кроме
телефона — не из списка WORLD.md).

### Технические заметки для реализатора

- Холсты мыслей разного формата: L1 — 3840×2160 с большими пустыми полями,
  L3 — 1920×1080, L2 — обрезанные по содержимому. При импорте в Unity
  кадрировать по содержимому (или запросить у дизайнера единый обрезанный
  экспорт). Силуэты с мягким размытым краем — это стиль, не артефакт.
- Спрайты объектов отданы в 2–4× игрового размера — масштабировать вниз,
  для 1080p запас нормальный.
- L3: для наполнения сосуда нужен либо отдельный спрайт сумки пассажира,
  либо оверлей-индикатор поверх запечённого фона — запросить у дизайнера
  или решить на превью-стенде.
- `.DS_Store` в папках не коммитить.

### Оставшиеся вопросы (к дизайнеру, не блокируют)

1. Отдельный спрайт сумки пассажира L3 (для наполнения сосуда) — или
   реализатор делает оверлей-индикатор.
2. Единый обрезанный экспорт мыслей (сейчас холсты разного формата).

---

## S1. Титул

- Фон: сцена уровня 1 без деталей, по ней медленно дрейфуют 3–4 мысли.
- Название «МЕДИТАЦИЯ В СПЕШКЕ»: центр (960, 300), кегль ~120.
- Две карточки-пиктограммы 360×240, y=520: левая (740) «КРУТИ — собирай
  детали» (иконка ручки), правая (1180) «ТРЯСИ — отгоняй мысли» (иконка
  джойстика с линиями тряски).
- Низ (960, 900): «Крути ручку, чтобы начать» — старт по 2 оборотам динамо
  *(тематично: игра начинается с медленного кручения)*.

## S2. Карточка уровня (интерстишл, 2.5 с, авто)

- Тёмный фон; центр: «Уровень N — <название>» (960, 460, кегль 72);
  под ним силуэт сосуда уровня (960, 620) и пустые слоты деталей
  (по числу деталей уровня).

## S4. Победа уровня (3 с, авто → S2 следующего)

- Мысли растворяются (0.5 с), сцена чистая 1 с — «тишина».
- Сосуд плавно в центр (960, 540), масштаб ×2, в нём все детали уровня.
- Текст под сосудом: «Собрано: <сеттинг>» + заполненные слоты.

## S5. Поражение

- Экран целиком закрыт мыслями, лёгкая десатурация.
- Центр (960, 480): «Мысли захватили всё. Вдохни.»
- Ниже (960, 620): «Крути ручку — попробуй снова»: каждый оборот динамо
  стирает ~10 % мыслей с экрана; очистил — рестарт уровня (S2 того же уровня).
  *(Ретрай тоже медитативен — предложение, [toggle: авто-ретрай через 4 с].)*

## S6. Финал (после последнего уровня — пока уровень 3)

- Сосуды пройденных уровней в ряд по центру, y=540 (пока 3: портфель,
  рюкзак, сумка — x = 480 / 960 / 1440), каждый со своими деталями.
- Текст (960, 300): «Ты заметил(а) всё. Даже в спешке.»
- Через 20 с бездействия или по любому вводу — чистый выход в лаунчер.

---

## Общие правила

- Переходы между экранами: фейд в чёрный 0.3 с.
- Кнопка «в меню»: из любого состояния — мгновенный чистый выход (контракт).
- Бездействие (нет ввода) 60 с на любом экране кроме S3 → выход в лаунчер
  **[открытый вопрос: есть ли общий idle-контракт стойки?]**
- Всё текстовое — русский; текст крупный (музейная дистанция ~1 м).
